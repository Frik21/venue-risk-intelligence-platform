# VenueGuard Product Authority

The Product Constitution (`docs/Product-Constitution.md`) is the highest authority.

Engineering must never override product decisions.

If implementation conflicts with the Constitution:

- STOP.
- Explain the conflict.
- Wait for approval.

Never silently redesign VenueGuard. Engineering serves the product. The Product Constitution always wins.

# Product Vision & Business Model

VenueGuard is the product name (still subject to change later, per direct product direction). The company behind it is **Vivarium** - surfaced today as a "Powered by Vivarium" attribution in the landing page's logo intro animation (`pages/landing.tsx`), the one place this is currently referenced in the app.

VenueGuard has two sides, each with its own name (surfaced in-app, e.g. `components/layout.tsx`'s sidebar header and `pages/dashboard.tsx`'s brand menu):

- **"Command Desk"**: the full admin app (Tasks, Quotes, Invoices, Clients, Vendors, Operator Database, Users, etc.) - used by Manager/Finance/Human Resources/Operations staff.
- **"Operators note"**: the CPO-facing field app (the Operational Canvas, at `/cpo`) - a separate product surface with its own branding, used by CPOs.

Going live, every company subscribes to one of two plans (see "Single-plan per-role seat model" and "Solo Operator plan" in Notes & Follow-ups below for the built mechanics):

- **Team** - the default. Command Desk + Operators note. Manager/Operations/Finance/Human Resources/CPO seats, each with a fixed free base and per-role additional seats trackable beyond it (CPO base 12, tracked as its own separate pool - see "CPO seats now have a real base+additional model too" in Notes & Follow-ups below).
- **Solo Operator** - a single freelance CPO's own subscription. Operators Note only, no Management side at all (technically enforced, not just hidden) - exactly one CPO account. Onboarded either from the Owner Console or via self-serve signup at `/register` (see "Self-serve Solo Operator signup" in Notes & Follow-ups below).

Longer-term, **Operators note is also planned as a fully standalone downloadable product** for individual freelance CPOs to use entirely independent of the rest of the system - not tied to any company's subscription at all. The Solo Operator plan above is a step toward that (a CPO-only subscription exists now), but it's still a company row on the same platform with an Owner-onboarded account, not the standalone downloadable app itself - that remains a separate, bigger, not-yet-built vision item.

**This is why CPOs are treated as a completely separate, seat-limited pool from Management-side Users** (Admin/Manager/Finance/Human Resources/Operations, managed at `/admin/users`) - a CPO isn't an admin-managed role, they're inventory tied to the subscription, and eventually a product of their own. CPOs are onboarded and tracked via Operator Database instead of Users. **CPO seat counting/limits are explicitly deferred** - per direct product direction, only the four Management-side roles have base+additional seat tracking today; CPO seats remain untouched until revisited as its own task.

**Billing/standalone-build mechanics don't exist yet** - additional seats are tracked as numbers only, nothing is actually billed, and standalone packaging for Operators note isn't built. This section is confirmed product direction to design *around* and never contradict, not a spec to build from unprompted - treat it the same as the Product Constitution above: implement pieces of it only when explicitly asked, and if an unrelated task would conflict with this vision, STOP and flag it rather than deciding silently. Revisit as its own dedicated project when asked.

# Workflow

Before every implementation task in this repo:

1. Create a feature branch.
2. Implement the requested task.
3. Run build (`pnpm run build`).
4. Run Constitution Compliance Check against `docs/Product-Constitution.md`.
5. Commit.
6. Push.
7. Open a Pull Request.
8. Wait for merge approval.

Report on completion:
- Branch
- Commit
- Build status
- Files changed
- Constitution compliance
- Pull Request URL

# VenueGuard Merge Policy

Never merge directly into main unless explicitly instructed.

# Session Continuity

If this session is starting fresh - no memory of prior conversation in this repo (e.g. a brand new session, or context that was compacted/summarized) - say so explicitly to the user before doing anything else. Don't silently improvise commands or workflows from guesswork.

The command to visually check the site (build + serve, single command, run by the user in their own terminal after a `git pull` of the working branch). `serve` is a root workspace script - it only exists at the repo root, not inside `artifacts/api-server` or `artifacts/risk-assessments`, so the `cd` to repo root is not optional:

```
cd /workspaces/venue-risk-intelligence-platform
git pull origin <branch>
pnpm run serve
```

Always give this exact command, with the repo-root `cd` included every time regardless of what directory the user's prompt shows them in - for visual verification. Don't substitute `pnpm run dev`, `pnpm run dev:all`, or a manually-assembled scratch-DB/Playwright setup unless the user explicitly asks for something different.

# Notes & Follow-ups

- **City/town database ceiling**: the search bar's city data (`city-registry.ts`) comes from Natural Earth's populated-places dataset, capped at 7,342 places worldwide - real cities and towns, but not exhaustive (e.g. Stellenbosch isn't in it). Come back to this later if we want a genuinely bigger database - likely means switching to a much larger source (e.g. GeoNames) and probably a real search backend instead of a static file shipped to the browser, since that kind of dataset is much bigger than what fits in a static array.
- **"Task Pending Quotation"**: on the Quotations page (`costs.tsx`), between the Quotes ledger and the Quotation Workspace - lists tasks with no formal Quote linked yet (`quotes.taskId` is null for that task). Its "Create Quote" button opens the Create Quote dialog pre-filled from the task (client/venue/requirements/dates/resources/manager), and the task drops off this list once that quote is saved.
- **"Vendors"**: now a full CRM (`/admin/vendors`) - onboard/track security subcontractors and suppliers, per direct product direction ("CRM style... onboard new security vendors"). Same structural pattern as Clients (profile card + dated activity log, detail page at `/admin/vendors/:id`) but deliberately without Clients' day/night rate or task/quote rollup fields - per direct product direction, a vendor is who VenueGuard pays/contracts with, not who it bills, so that CRM data model is a different shape. Schema: `vendors` + `vendor_activities` tables (`lib/db/src/schema/vendors.ts`, `vendor-activities.ts`).
- **"Communications"**: a real Manager-to-CPO broadcast (`/admin/communications`) - one-way, no chat, per direct product direction. Optionally task-connected: when posting, a Manager can pick a task from a dropdown (default "General - all operators"); picking one scopes the announcement to that task's roster (via `task_assignments`, the same roster-aware logic used by Tasks/Deployment/Schedule) - only CPOs on that task see it, shown with a `T-000x · title` badge. Leaving it "General" broadcasts to every CPO like before. Filtering happens frontend-side (`visibleAnnouncements` in `dashboard.tsx`, comparing each announcement's `taskId` against the current CPO's own task list). `announcements.taskId` is nullable with `onDelete: set null` (not cascade) so a message stays around as a general note if its task is later deleted. Completes pre-existing scaffolding already in the CPO Operational Canvas (`dashboard.tsx`) - its Communications panel (opened from the "Operators note" brand menu) previously showed hardcoded demo instructions, now shows real, roster-filtered announcements. Instructions also surface in the CPO's Alerts panel (`instructionAlerts`, mapped from the same filtered data) since a Manager instruction is exactly the kind of thing an operator shouldn't have to go looking for separately. Schema: `announcements` table (`lib/db/src/schema/announcements.ts`).
- **"Invoices"**: a real client-billing entity (`/admin/invoices`) - "money owed to you", per direct product direction. Own lifecycle (draft/sent/paid), separate from and simpler than Quotes - billing line items + a single tax rate, no markup or derived internal-cost build-up, since an invoice bills an already-agreed amount. "Task Pending Invoice" lists completed, non-invoiced tasks (same pattern as Quotations' "Task Pending Quotation") - its "Create Invoice" button pre-fills from the task, and from an approved Quote on that task if one exists. **Approving a Quote now auto-creates a draft invoice** (`routes/quotes.ts`'s PATCH handler, on the draft/sent→approved transition, guarded against duplicates by checking for an existing invoice with that `quoteId`) - a single uncategorized line item for the quote's `totalQuoteValue`, same client/tax/currency, per direct product direction. A Manager can then add further **categorized** line items on that draft for costs incurred beyond the quoted amount (operational costs, additional manpower, vehicles, etc.) - reuses Quotes' own cost category vocabulary (`COST_CATEGORIES`/`QUOTE_COST_CATEGORIES`, exported from `routes/quotes.ts` / `lib/api.ts`) so both entities share one taxonomy; `category` is nullable per line item (`null` = the original agreed amount, a real category = an added cost). Saving an invoice against a task marks that task `invoiced` server-side (same sync the auto-create path also triggers), which is what moves it from the Completed to Invoiced bucket on the Tasks list (see PATCH/POST `/invoices` in `routes/invoices.ts`). Schema: `invoices` table (`lib/db/src/schema/invoices.ts`), `lineItems` now `{category: string | null; description; amount}[]`. PDF via `lib/invoice-pdf.ts` (renders category as a `[Label]` prefix on the description).
- **"Payroll"**: now real CPO pay runs (`/admin/payroll`) - "money you owe operators", per direct product direction. Rolls up approved timesheet hours (same rate x hours math as Personnel Costs on the Quotations page - see `lib/personnel-cost.ts`) per operator into a "Pending Payroll" total; "Create Pay Run" snapshots that total into a `pay_runs` row (pending -> paid, "Mark Paid" stamps `paidAt`) and stamps every covered `timesheet_entries` row with that run's id (new `pay_run_id` column, `onDelete: set null`) so the same hours can never be paid out twice - deleting a run puts its hours back into pending. No currency label, matching Personnel Costs' existing convention (CPO rates don't carry their own currency). Schema: `pay_runs` table (`lib/db/src/schema/payroll.ts`).
- **Multi-office scoping**: Tasks, Quotes, Invoices, Clients, Vendors, and Users (Managers & CPOs) each carry an optional `officeId` FK to the pre-existing `offices` table, per direct product direction ("companies will have different offices... select an office and all the data from the allocated office"). A global switcher at the top of the Manager sidebar (`layout.tsx`, "All Offices" + one entry per office) is the single source of truth for which office is currently selected - persisted to `localStorage` and broadcast via a custom event so every page reacts without prop-drilling (see `lib/office-scope.ts`: `useSelectedOfficeId()`, `setSelectedOfficeId()`, `filterByOffice()`). Every list page (Tasks, Quotations' Quotes ledger, Invoices, Clients, Vendors, Users) filters to the selected office client-side; "All Offices" shows everything, including records with no office set (a record with `officeId: null` only ever shows under "All Offices", never implicitly claimed by any one office). Every creation dialog for these six entities (`new-task-dialog.tsx`, `quote-dialog.tsx`, `invoice-dialog.tsx`, `clients.tsx`'s `ClientDialog`, `vendors.tsx`'s `VendorDialog`, `users.tsx`'s `NewUserDialog`) defaults its own Office field to whatever's currently selected, editable per-record. `users.officeId` and `offices.managerId` reference each other, which needed an explicit `AnyPgColumn` return-type annotation on both `references()` callbacks (`schema/users.ts`, `schema/offices.ts`) to break a TS circular-inference error Drizzle otherwise can't resolve on its own. The "Add/Edit Office" dialog's Office Manager field (`offices.tsx`) can create a brand-new Manager inline ("+ Add New Manager" - a name/email mini-form right under the dropdown, `POST /users` with `role: "manager"`) rather than only letting you pick from Managers that already exist - matters most on a fresh install where no Manager exists yet to assign.
- **Dashboard Trends**: the Management Dashboard (`/admin`) now has a "Trends" section with a "Since" date picker (default 30 days back) and six line charts - Tasks Completed, Quotes Sent vs Pending, Invoices Pending, New Clients Onboarded, Operators Onboarded - plus two live StatTiles, Tasks Running and Operators on Tasks. The StatTile/line-chart split is deliberate: the two StatTile metrics only have a *current* value in the database (no way to know what they were on a past day), so they're a snapshot number rather than a fabricated trend line; everything else is a real reconstructed history from timestamps already in the schema - no new daily-snapshot table. Two shapes of trend, both in `lib/trend-buckets.ts`: "throughput" (`countByDay`) counts a stamped-once timestamp per bucket day (`tasks.completedAt` - new column, stamped like `sentAt`/`decidedAt` elsewhere, the first time status moves into `completed`; `quotes.sentAt`; `clients.createdAt`; `operator_onboarding.operationalAccessGrantedAt`); "pending window" (`countOpenByDay`) counts records open on each day via two timestamps bracketing the open period (`quotes.sentAt`→`decidedAt`, `invoices.sentAt`→`paidAt`) - both were already on the schema, so Quotes Pending/Invoices Pending get a truthful backlog-size-over-time line for free. Charts render with `recharts` via the existing shadcn `ChartContainer`/`ChartTooltip` wrapper (`components/ui/chart.tsx`) through a new shared `<TrendChart>` (`components/trend-chart.tsx`). Respects the office switcher like every other admin page (Tasks/Quotes/Invoices/Clients/Users filtered via `filterByOffice`) - this also retrofit office-scoping onto the Dashboard's pre-existing sections (Task Assignment, Operators in the Field), which hadn't been touched when multi-office scoping first shipped. Operator Onboarded stays unfiltered since `operator_onboarding` isn't one of the office-scoped entities.
- **Role Select landing page (`/`) - 3 more Quick Access tiles**: added "Operations", "Finance", and "Human Resources" alongside the existing "CPO" and "Management" (renamed from "Admin" per direct product direction) tiles (`role-select.tsx`, now a data-driven `TILES` array, grid widened to `lg:grid-cols-3`/`max-w-5xl` to fit five). Per direct product direction, all three new tiles link to the same `/admin` Management Dashboard that "Admin" already does - they're just extra entry points into the one existing dashboard for now, not separately role-scoped views. Revisit if/when role-scoped dashboards (e.g. Finance only seeing Quotations/Invoices/Payroll) are wanted - flagged as a bigger, separate build when asked, not assumed.
- **CPOs can no longer reach the Management Dashboard from inside their own app**: the CPO Operational Canvas ("Operators note", `/cpo`) had one real escape hatch - an "Admin Dashboard" item in the top-right operator menu that navigated straight to `/admin`. Per direct product direction ("CPOs can only access Operators Note"), removed that menu item (`dashboard.tsx`'s `TopBanner`), leaving Profile and Sign Out. Note: this app has no real login/authentication yet (a single hardcoded "Senior Analyst" user, plus a manual "Operator View" toggle link in the Manager sidebar to preview the CPO experience) - this is a UI-level restriction on the one in-app navigation path a CPO had, not server-enforced access control; typing `/admin` directly in the URL still works, same as before. Revisit with real role-based auth if that boundary needs to be load-bearing.
- **Users page redefined: Manager/Finance/Human Resources/Operations, no CPOs**: `/admin/users` ("Users, Roles & Permissions") now manages five roles - Admin (VenueGuard's own platform-owner role, kept per direct product direction) plus Manager, Finance, Human Resources, and Operations (the four Management-side roles, matching the four non-CPO Quick Access tiles). CPOs are no longer creatable, visible, or manageable from this page at all, per direct product direction ("users are not for cpos") - they're a separate, seat-limited pool onboarded via Operator Database instead (see "Product Vision & Business Model" above for why: a different subscription-tier-gated resource entirely, not an admin-managed seat). `users.tsx` filters `allUsers` by `role !== "cpo"` before anything else renders (`ROLE_COLORS`/`ROLE_ICONS`/`ROLE_LABELS` now typed `Partial<Record<UserRole, ...>>` since "cpo" is still a valid `UserRole` in the data model, just never looked up here); the role-count tile grid widened from 3 to 5 columns. The `UserRole` type (`lib/api.ts`) and the backend's role `z.enum` (`routes/users.ts`) both gained `"finance" | "human_resources" | "operations"`, and `dashboard.tsx`'s `USER_ROLE_LABELS` (Profile panel display) picked up matching entries via TS's exhaustive `Record<UserRole, string>` typing. The CPO day/night pay-rate editor that used to live here (`CpoRateEditor`) was removed along with CPO rows - see the Operator Database note below for where it moved.
- **Fixed: Role Select landing page (`/`) wasn't full-screen**: it rendered as a floating dark card with light-gray margins around it - `role-select.tsx` was capped at `min-h-[80vh] rounded-3xl`, and the shared `Layout` wrapper (`components/layout.tsx`) still applied a `max-w-7xl mx-auto` constraint to the content area even on `hideShell` routes (`/` and `/cpo`, the two routes that already hide the sidebar/header/padding for a full-bleed look). Fixed by changing Role Select to `min-h-screen` with no rounding, and making the `max-w-7xl mx-auto` wrapper conditional on `!hideShell` so both full-bleed routes actually reach the screen edges; verified `/admin` (not a `hideShell` route) is unaffected and `/cpo`'s own dark card still renders correctly edge-to-edge.
- **Fixed: unassigned operators no longer duplicate across both onboarding columns**: on `/admin/onboarding`, an operator who hasn't yet had their "Freelancer" or "Long Term Contract" checklist item checked used to render as a duplicate card in *both* the Freelance Database and Long Term Contract Database columns at once. Per direct product direction, an unassigned operator (pending documents/checklist, no engagement type picked yet) shouldn't appear in either column at all - findable instead via the "Pending" status-filter view. `freelancerColumnRecords`/`longTermColumnRecords` (`onboarding.tsx`) now filter strictly by assignment instead of the old "include if unassigned OR matches" logic, so an operator only ever shows in the two-column view once actually assigned. (This initially left approved-but-unassigned operators unreachable - see the follow-up fix below.)
- **Fixed: couldn't re-onboard a previously-removed operator**: `POST /onboarding` (`routes/onboarding.ts`) used to 409 on ANY existing `users` row with that email, including inactive ones. `DELETE /onboarding/:id` doesn't actually delete the linked `users` row - it deactivates it (`active: false`), deliberately, so the operator's historical tasks/timesheets aren't cascade-deleted. Since `users.email` is `UNIQUE`, that inactive row permanently blocked re-onboarding the same email - reported as "I cannot add another operator." Fix: only block (409) when the existing user is genuinely `active`; if inactive, the new `operator_onboarding` insert re-links to that same user id instead of trying to insert a second row with a duplicate email. A defensive check blocks (409) if that inactive user is somehow still linked to another live onboarding record (`operatorOnboardingTable.userId` is itself unique) before re-linking. Verified end-to-end through the actual UI: add operator → approve → grant operational access → remove → re-add same email succeeds with no error, correctly reactivating the same underlying user record.
- **CPO pay rate moved to Operator Database; Pending/Denied made "action needed" buckets**: two related changes on `/admin/onboarding`. (1) The CPO day/night rate editor (removed from Users - see that note above) now lives in each CPO's "Manage" panel as `OperatorRateEditor`, gated on `record.userId != null` (a real account must exist first) - looks the linked user up from the shared `["users"]` react-query cache rather than fetching per-record. `operationalAccessMutation`'s `onSuccess` now also invalidates `["users"]`, since the *first* grant creates that user row for the first time - without this the rate editor's lookup would silently find nothing right after granting access. (2) While verifying that flow, found that an operator who's Approved but still hasn't been assigned Freelancer/Long Term Contract had become completely unreachable: not in either column (unassigned), and no longer under "Pending" either since that only matched `status === "in_progress"` and this one had moved to "onboarded". Per direct product direction, redefined `isPending` (`onboarding.tsx`) as "not-yet-approved (`in_progress`) OR approved-but-still-unassigned" - Denied stays a pure, separate status filter, deliberately excluded from that broadened definition so a denied-and-unassigned candidate only ever shows under Denied, never bleeds into Pending. Denied also needed its own flat-list render (same treatment as Pending) instead of the two-column split, for the identical reason - the two-column view drops unassigned records regardless of which status filter is active. Verified the full matrix live: unassigned+pending, unassigned+approved, unassigned+denied, and assigned+approved all land in exactly the right (and only the right) view, and the rate editor round-trips correctly (save → reload → value persists).
- **Multi-tenant company scoping + Owner Console**: VenueGuard's first real multi-tenancy layer. New `companies` table (`lib/db/src/schema/companies.ts` - `id`, `name`, `tier: "enterprise"|"micro_enterprise"`, `status: "trial"|"active"|"suspended"|"cancelled"`, `createdAt`); every other table gained a `company_id` (direct FK on root entities like `users`/`offices`/`venues`/`clients`/`tasks`/`quotes`/`invoices`/`vendors`, denormalized-but-physical on child tables like `client_activities`/`task_assignments`/`timesheet_entries`). Repurposed the existing `admin` role into VenueGuard's own platform-Owner role rather than adding a 7th role - per direct product direction ("i cannot have access to any of the subscribers pages" after launch) - removed as a selectable option from `/admin/users`'s picker, `companyId: null` reserved exclusively for it. One-off `scripts/src/backfill-company-id.ts` seeded a "Default Company" absorbing all pre-existing data and exactly one Owner account (`owner@venueguard.internal`), before every `company_id` column got flipped to `NOT NULL`. New aggregate-only `GET/POST/PATCH /companies` API (`routes/companies.ts`) and Owner Console page (`/owner`, `pages/owner/dashboard.tsx`) - lists subscriber companies with tier/status/seat counts/signup date/last activity, but by construction never returns row-level tenant content, only the `companies` table's own metadata plus `count()`/`max()` aggregates grouped by `company_id`. Company-scoping for regular Management pages initially worked exactly like the pre-existing Office-scoping pattern (client-side `company-scope.ts` selector) - later removed once real auth made server-side scoping the actual boundary (see below).
- **Real authentication**: email + password login with server-side sessions, closing the gap the multi-tenancy work above deliberately left open (client-side-only company scoping). New `sessions` table (`lib/db/src/schema/sessions.ts` - opaque random `id`, `userId`, `expiresAt`, `lastSeenAt`; no `company_id` column, tenancy resolved live via a join to `users` so a company reassignment takes effect immediately) plus `users.passwordHash`/`mustChangePassword`. Hand-rolled over `express-session` on purpose - matches this codebase's existing preference for small explicit helpers (`resolveCompanyId`, `apiFetch`) over framework abstractions: `lib/auth.ts` (bcryptjs hashing, session CRUD, `requireAuth`/`requireRole` middleware) + `routes/auth.ts` (`login`/`logout`/`me`/`change-password`), using `cookie-parser`'s signed-cookie mode (a dependency that had sat unused in `api-server`'s `package.json` since before this). `routes/index.ts` registers `auth`/`health` unauthenticated, then gates every other router behind one `router.use(requireAuth)` line rather than 33 file-by-file edits. Closed two real gaps the multi-tenancy work exposed: write paths previously trusted a client-supplied `companyId` (now reads `req.user.companyId` from the session instead - `resolveCompanyId` itself unchanged, just a trusted argument now), and most GET/list routes applied no company filter at all (fixed across the primary flat list endpoints, `users.ts`/`tasks.ts` first as the reference pattern). New users (via `/admin/users` or the onboarding operational-access grant) get a server-generated one-time password shown once in the response, `mustChangePassword: true` forcing a change-password step on first login - no email infrastructure exists yet to send a real invite. A one-off `scripts/src/set-owner-password.ts` bootstraps the seeded Owner's first password the same way, since nothing else could. Frontend: `lib/auth.tsx` (`AuthContext`/`useAuth`), `pages/login.tsx`, `components/require-auth.tsx` (route guard - unauthenticated → `/login`, `mustChangePassword` → `/change-password`, role-based post-login routing: `cpo` → `/cpo`, Owner → `/owner`, everyone else → `/admin`), and the previously-hardcoded "Senior Analyst" (`layout.tsx`) / `"frik"` name-match (`dashboard.tsx`'s `profileUser`) identity placeholders now reflect the real logged-in session.
- **Owner Preview mode**: the mechanism that actually lets the Owner browse the Management/CPO pages for QA without violating "no access to real subscribers' pages" - one company can be marked as the **Test Company** (a toggle on `/owner`'s companies table), and only that one company's "Preview" button does anything; every other company's Preview is inert by design, not just hidden. Clicking Preview (`POST /auth/preview/:companyId`, `routes/auth.ts`) stamps the Owner's *own* session with a temporary `previewCompanyId` (not a new user, not impersonation of a real account) - `requireAuth` resolves `req.user.companyId` from that field when present, so every existing company-scoped route just works, and `req.user.isPreviewing: true` flows through to the frontend via `GET /auth/me`. A violet "Previewing X - not a real subscriber" notice renders inline in `Layout`'s own header (originally tried as a separate `fixed`/`sticky` banner - see the two fix notes below for why that didn't work) and in the CPO canvas's operator menu, each with an "Owner Console" link (returns to `/owner` without ending Preview) alongside "Exit Preview" (`POST /auth/preview/exit`, ends it). `role-select.tsx`'s old "Where do you want to go?" chooser was repurposed as a Preview-mode fast-path, moved off the default `/` landing spot to `/quick-access`, and made Owner-only (`require-auth.tsx` redirects any non-admin session away from both `/` and `/quick-access` to their own home route) - it's a manual shortcut into whatever the current Test Company's pages look like, not a real role-select gate.
- **Fixed: Preview banner not appearing**: a `fixed`-then-earlier-`sticky` banner rendered as a sibling above `Layout`'s own `sticky top-0` sidebar/header competed for the same screen position rather than cleanly stacking - the banner could be in the DOM but never actually visible, reported directly as UI elements "duplicating." Resolved by moving the notice into `Layout`'s existing header bar as normal in-flow content instead of a separate positioned layer.
- **Company/Office switcher visibility tightened**: reported directly as confusing duplication with the real "Offices" management page in the nav. The Company switcher is now Owner-only, full stop (not shown to any real subscriber's session, not even as a locked label) - client-side `company-scope.ts`/`filterByCompany()` turned out to have zero real consumers once server-side scoping (the auth work above) existed, so both were deleted rather than left as dead code. The Office switcher only renders once a company actually has more than one office - with a single office it's indistinguishable from "All Offices" and just duplicated the real Offices page in the nav below.
- **Quick Access placeholder tiles**: `/quick-access` (see Owner Preview mode above) got an "IT" tile and four more - "Landing Page," "Subscriptions," "Enterprise," "Single Operator" - each with `href: null`, rendered as a dashed, non-interactive "Coming soon" card rather than silently landing on `/admin` like the real department tiles do. Tracks real gaps already flagged this session (no public marketing/signup page exists yet - a potential client hitting the live site today gets redirected straight to `/login` with no way to create an account; no billing/subscription-tier UI; Operators note's planned standalone single-CPO product, per the "Product Vision & Business Model" section above) without building any of them prematurely. The "Landing Page" tile now links to the real thing (see below) rather than sitting as a placeholder. "Subscriptions" later got wired to `/owner` too, once the Solo Operator plan note below shipped what it was standing in for; "IT," "Enterprise" (stale now that tiers are gone), and "Single Operator" (the bigger, still-unbuilt standalone-downloadable-app vision item, not the same thing as the Solo Operator plan) remain placeholders.
- **Public landing page**: `/` now shows a real public marketing page (`pages/landing.tsx`) instead of bouncing straight to `/login` - the first thing a stranger clicking through from a search result actually sees. Shows for authenticated sessions too, not just logged-out visitors - being logged in shouldn't change what the homepage itself shows, same as any real marketing site (`require-auth.tsx` renders it directly for `location === "/"` regardless of auth status, before falling through to the usual role-based redirects). Content pulled from real, already-built features and the Product Constitution's own Mission/North Star/30 Second Rule language, not invented copy. Opens with a short (1.5s) logo intro animation (shield + wordmark, staggered fade/scale via `framer-motion`) that crossfades into the page, with a "Powered by Vivarium" attribution in the corner - VenueGuard is the product name (still subject to change later), Vivarium is the company behind it, the one place that's currently surfaced in the app. Two CTAs throughout - "Register" and "Log In".
- **Self-serve company registration**: `POST /auth/register` (unauthenticated, alongside login/logout) is the first real way into VenueGuard that doesn't require an existing Owner or Manager to onboard someone by hand - closes the "Self-serve company signup" roadmap item below. Creates a new company (`status: "trial"`, same default as every other company-creation path) and its first user as `role: "manager"` (the "runs their own company" role among the four company-side roles - `admin`/Owner is never reachable from this endpoint). Unlike admin-created users, the password is the one the person just typed at signup, so there's no `initialPassword`/`mustChangePassword` step - they're session-cookied and logged straight into `/admin`. Frontend: `pages/register.tsx` (company name, per-role additional-seat inputs - see the seat-model note below, name, email, password), wired into the same auth routing (`App.tsx`/`require-auth.tsx`/`layout.tsx`) as `/login`; `useAuth()` gained `register()` alongside `login()`/`logout()`.
- **Single-plan per-role seat model** (replaces Enterprise/Micro Enterprise tiers): per direct product direction ("I want just one [plan]... if they do want additional seats they must be able to select how many"), `companiesTable` lost its `tier` column and gained 4 integer columns - `additionalManagerSeats`/`additionalOperationsSeats`/`additionalFinanceSeats`/`additionalHumanResourcesSeats` (`lib/db/src/schema/companies.ts`). Every company gets the same fixed free base per Management-side role - Manager 8, Operations 5, Finance 5, Human Resources 5 (`BASE_SEATS_BY_ROLE`, deliberately duplicated with an explicit sync-comment between `routes/companies.ts` and the frontend's `lib/api.ts` rather than pulled into a shared package, matching this codebase's small-explicit-helper convention) - and each role tracks its own additional-seats-purchased count independently (not a shared pool across roles). "Seats" = Management-side user accounts created under that role (`users.role`); a company can go over its base per role, with the overage tracked as a number for later billing - no billing integration or enforcement exists yet, this is bookkeeping only. **CPO seats are explicitly untouched/out of scope** - `cpoCount` on the Owner Console still shows with no base or limit, per direct product direction to come back to CPO seat mechanics separately. Surfaces: `/register`'s new "Seats" section (per-role `+N` inputs next to each role's base, replacing the old tier `<Select>`), and the Owner Console (`/owner`) - `NewCompanyDialog` uses the same per-role inputs, and each company row's "Seats" column shows a compact `Manager X/Y · Operations X/Y · Finance X/Y · HR X/Y · CPO N` breakdown with a gear-icon `EditSeatsDialog` to adjust a company's additional seats per role after signup. `GET /companies/summary`'s "Est. Monthly Revenue" figure switched from a per-tier price map to a flat base price plus a flat price per additional seat (still a hardcoded placeholder, not real pricing - see Outstanding below). `scripts/src/backfill-company-id.ts`'s idempotent raw-SQL `CREATE TABLE IF NOT EXISTS companies` fallback updated to match (drops `tier`, adds the 4 seat columns + `is_internal`, all defaulted) so a from-scratch production run stays consistent with the Drizzle schema.
- **Solo Operator plan**: a second, real subscription plan alongside Team - per direct product direction ("Solo Operator will only have access to 'Operators Note'"). `companiesTable` gained `planType` (`"team"` default, or `"solo_operator"` - `lib/db/src/schema/companies.ts`), decided at company creation and not changed after. Access is **technically enforced, not just hidden**, at both layers: `require-auth.tsx` redirects any `planType: "solo_operator"` session to `/cpo` for any other route (except `/change-password`), and the backend's new `blockSoloOperatorFromManagement` middleware (`lib/auth.ts`, mounted once in `routes/index.ts` right after `requireAuth`) 403s any API path outside a small allowlist (`/weather`, `/traffic`, `/announcements`, `/users`, `/tasks`, `/alerts`, `/venues`, `/plans`, `/countries`) - built from the actual set of endpoints the CPO Operational Canvas (`pages/dashboard.tsx`) calls, verified by direct code search rather than assumed, so nothing the real CPO app needs was accidentally left off. `requireAuth` now joins `companiesTable` to resolve the effective session's `planType` onto `req.user` (respecting the same Preview-mode override as `companyId`), and `formatSessionUser` (`routes/auth.ts`) rides it along on `GET /auth/me` the same way `companyName` already does. No self-serve signup path yet - a Solo Operator company is onboarded entirely from the Owner Console: `NewCompanyDialog`'s new Plan selector (Team/Solo Operator) swaps the per-role seat inputs for a CPO Name/Email pair when Solo Operator is picked, chains a `POST /companies` then a `POST /users` (`role: "cpo"`) in one submit, and shows the generated one-time password once (same pattern as `admin/users.tsx`'s `NewUserDialog`). **Solo Operator is for an individual, not a company** - per direct product direction, the dialog never asks for a separate "company name" when this plan is picked; the underlying `companies` row (still required, since `company_id` is how every table is tenant-scoped platform-wide) is just named after that person directly. Dialog title/button/copy are plan-aware ("Onboard Company" vs "Onboard Solo Operator"), and the main table's "Company" column was renamed "Subscriber" (with the top-level onboarding button/empty-state copy following suit) since a row can now represent either a company or an individual.
- **Owner-editable subscription pricing**: the Owner Console's "Est. Monthly Revenue" stat tile is now a clickable "Subscription" button opening `SubscriptionPricingDialog` - per direct product direction ("I need to be able to see what are the subscriptions and their prices, I must also be able to select what the prices must be"). Shows both plans (Team: base price/month + price per additional seat, with the fixed seat bases shown read-only for context; Solo Operator: a single flat price/month) with editable number inputs, plus the current Est. Monthly Revenue figure for context. Backed by a new platform-wide singleton table, `pricing_config` (`lib/db/src/schema/pricing-config.ts` - not company-scoped, always exactly one row, same get-or-create-on-first-read pattern `company_settings`/`routes/settings.ts` already established) - replaces what used to be hardcoded `BASE_MONTHLY_PRICE`/`PRICE_PER_ADDITIONAL_SEAT`/`SOLO_OPERATOR_MONTHLY_PRICE` constants in `routes/companies.ts`, which now reads/writes this table instead (`GET`/`PATCH /companies/pricing`, Owner-only like the rest of that router). Still directional only - no payment processor is connected, this only controls what the Owner Console's own estimates show. Seat bases (`BASE_SEATS_BY_ROLE`) stay fixed/non-editable here on purpose - this dialog is about dollar amounts, not seat counts, which remains its own separate, not-yet-decided product question (see "Seat-limit enforcement" in Outstanding below). Pricing: a flat `SOLO_OPERATOR_MONTHLY_PRICE` (placeholder, like the Team plan's own pricing constants) rather than the base+seats formula, since there's exactly one seat and no roles. The Owner Console table gained a "Plan" column and an "Est. Charge" column (per-company `estimatedMonthlyCharge`, shown regardless of status) - the latter also closes the "Owner Console subscription view" ask that kicked this off; a Solo Operator row's "Seats" cell shows just its one CPO count instead of the four-role Management breakdown (nothing to edit, so no gear icon). The dead "Subscriptions" tile on `/quick-access` now links to `/owner`, where this actually lives. **The "exactly one CPO" part is a hard cap, not just a convention of the onboarding dialog** - `POST /users` (`routes/users.ts`) rejects a second `role: "cpo"` creation against a company whose `planType` is `solo_operator` with a 409, since nothing else stopped the Owner from calling it a second time against the same company (the onboarding-flow's operational-access-grant path that creates CPOs for Team companies isn't reachable for a Solo Operator company at all, since Operator Database is Management-side).
- **Management side named "Command Desk"**: per direct product direction, to pair with the CPO-facing "Operators note" so both product surfaces have their own name. Surfaced in the same spot "Operators note" is - the Management shell's persistent sidebar header (`components/layout.tsx`, replacing the "Risk Intelligence" subtitle under the VENUEGUARD wordmark, the one place that text was scoped to the Management app itself rather than the shared login/register/landing pages, which keep "Risk Intelligence" as VenueGuard's own overall tagline). Not renamed anywhere else in code (routes, identifiers, the `/admin` path itself) - this is a display label, same as how "Operators note" itself was applied.
- **Command Desk tied in with Operators note (Team side only)**: per direct product direction, confirmed the Solo Operator plan stays fully standalone/unbranded-together (it has no Command Desk at all to tie into) - this is specifically about a Team company's Command Desk referencing the "Operators note" name its own CPOs are using, which the two brand names never did before. Two spots: the Manager sidebar's CPO-preview link (`components/layout.tsx`) now reads "Operators note" instead of the generic "Operator View" it said before, and the Operator Database page's subtitle (`pages/admin/onboarding.tsx`) now reads "...for every CPO using Operators note" instead of just "...for every CPO". Same display-label-only scope as the Command Desk naming above - no functional/navigation change.
- **CPO seats now have a real base+additional model too**: per direct product direction ("the seats allocated will be 12 with the option to add additional seats"), CPO seats (Operators note) are no longer untouched - they now follow the exact same base+additional shape as the four Management roles, but tracked completely separately (a fifth `additional_cpo_seats` column on `companiesTable`, `CPO_BASE_SEATS = 12`, exported from `routes/companies.ts`/`lib/api.ts` the same way `BASE_SEATS_BY_ROLE` is) since CPOs remain their own seat-limited pool per the Product Vision, not a fifth Management role. `buildCompanyRows()` now returns `cpoSeatUsage: {used, base, additional, limit}` alongside the existing `cpoCount`; the Owner Console table's CPO badge shows `used/limit` instead of a raw count (for Team companies - Solo Operator's single hard-capped CPO seat is unrelated and unchanged). New shared `CpoSeatInput` component (visually separated with an "Operators note" label, same pattern as the branding tie-in above) added to `/register`'s Seats section, `NewCompanyDialog`, and `EditSeatsDialog`. Additional CPO seats fold into the same `additionalSeatsTotal()` used for pricing - an extra CPO seat costs the same "price per additional seat" as an extra Management seat, no separate CPO pricing tier. Still bookkeeping only, same as the Management roles - no enforcement blocking a company from onboarding a CPO past its limit (seat-limit enforcement itself remains a separate, not-yet-decided roadmap item).
- **Seats section removed from `/register`**: per direct product direction, seat management is moving into Command Desk itself rather than being asked at public signup - the destination there isn't decided yet (flagged as a follow-up). `pages/register.tsx` dropped the whole Seats section (all five roles) along with its state - a self-serve company now always starts on base-only seat counts for every role, no additional seats settable at signup. `useAuth()`'s `register()` call no longer sends any `additionalXSeats` fields (the backend already defaults every one to 0 when omitted, so no route change was needed - `POST /auth/register`'s schema still accepts them, just unused from this form now). The Owner Console's own seat controls (`NewCompanyDialog`/`EditSeatsDialog`) are untouched - adjusting seats there still works exactly as before.
- **Fixed: Quick Access tiles silently bounced back to the Owner Console**: reported directly ("whenever I click on the quick access dashboard it takes me to the owners console"). Root cause: the CPO/Management/Operations/Finance/HR tiles on `/quick-access` (`pages/role-select.tsx`) link straight to `/cpo`/`/admin`, but a plain Owner session (not currently previewing a Test Company) has no company context there - `require-auth.tsx`'s existing catch-all correctly bounces that case back to `/owner` rather than showing a broken empty page, but did so silently with zero explanation, reading as a bug rather than expected behavior. Fixed by disabling those five tiles up front (same dashed "locked" treatment already used for the not-built-yet placeholders, but labeled "Start Preview first" instead of "Coming soon") whenever `!user.isPreviewing`, plus a one-line hint under the page header pointing back to the Owner Console's Preview button. Landing Page and Subscriptions tiles are unaffected - neither needs a company context.
- **Command Desk gets its own seat management - "+ Additional Seats" on `/admin/users`**: the space the seats-removed-from-`/register` note above said was still to be decided - now built. Next to "+ Add User", per direct product direction. Backed by a new self-service pair, `GET`/`PATCH /users/seats` (`routes/users.ts`) - deliberately not the Owner Console's aggregate-only `/companies` surface (admin-only), this is any Management-side role adjusting their own company's seats directly, same looseness this page already has ("No team grouping or granular per-user permissions exist yet"). Registered ahead of the pre-existing `PATCH /users/:id` route, which would otherwise swallow `/users/seats` by matching "seats" as its `:id` param. `AdditionalSeatsDialog` mirrors the Owner Console's own seat-editing UI (`SeatInputs`) but scoped to the session's own company, no company picker. The four role-count tiles at the top of the page now show real `used/limit` seat data (falling back to a plain count on first load, before that query resolves) instead of just a raw headcount. CPO seats deliberately excluded from this page - those stay on Operator Database, unrelated to Command Desk's own Users page.
- **Additional-seat cost now visible in Command Desk, not just the Owner Console**: per direct product direction ("this cost must show in the + add additional seats menu"). `GET`/`PATCH /users/seats` now also return `pricePerAdditionalSeat` (the same Owner-set number from `/companies/pricing`'s `pricingConfigTable`, via a newly-exported `getOrCreatePricingConfig` from `routes/companies.ts`) - not tenant-sensitive data, so safe to surface to a regular company session even though the rest of that pricing surface stays Owner-only. `AdditionalSeatsDialog` (`pages/admin/users.tsx`) now shows "$X/seat" next to each role's base, plus a live running total ("N × $X = $Y/mo") at the bottom that updates as the additional-seat inputs change, so a Manager sees the cost before saving rather than discovering it after the fact.
- **CPO seats added to Command Desk's own "+ Additional Seats" dialog too**: per direct product direction, this had shipped Management-roles-only. `GET`/`PATCH /users/seats` now also compute/accept `cpoSeatUsage`/`additionalCpoSeats` (`routes/users.ts`'s `buildSeats()`, renamed from `buildSeatsByRole` now that it covers both) - no plan-type check needed since this whole page is already unreachable for a Solo Operator company (`blockSoloOperatorFromManagement`), so a company that gets this far is always Team. `AdditionalSeatsDialog` shows the CPO row visually separated under an "Operators note" label (same treatment the Owner Console gives it), and it's included in the live running-cost total alongside the four Management roles - an extra CPO seat costs the same per-seat price, no separate CPO pricing tier. Also confirmed the billing framing directly: adding seats is a one-off configuration change, not a repeated purchase, but the cost then folds into the recurring monthly bill every month after (same math as the Owner Console's Est. Monthly Charge) - which is exactly how `additionalXSeats` already worked, just confirmed rather than changed.
- **"Buy" replaces "Save" on Command Desk's Additional Seats dialog**: per direct product direction - seats are something a company buys, not just configures. Since no payment processor exists yet, this is deliberately a stub (confirmed via AskUserQuestion): the button now reads "Buy - $Y/mo" (the live running total) and applies the new seat counts immediately on click, same mechanics as the old Save button, with an amber "No payment processor is connected yet - Buy applies the seats without an actual charge" note so it doesn't read as a real transaction. Deliberately left the button always-enabled (not disabled at 0 additional seats) since the same "Buy" action is also how a company would reduce back down to 0 - disabling it there would block that. The Owner Console's own seat-editing dialog (`EditSeatsDialog`) is untouched - that's an Owner override, not a purchase flow, so it keeps "Save". The "seats become available immediately, but the added recurring cost only shows up starting the next full billing cycle" part of the ask was deliberately NOT built - flagged as a roadmap item below instead, since there's no billing-cycle/invoice concept anywhere in the system yet to delay against (today's Est. Monthly Charge is a live snapshot, not a cycle-based figure) - real payment integration needs to exist before that delay logic means anything.
- **"Owner Console" renamed to "Master Console"**: per direct product direction. Display-label-only, matching how "Command Desk"/"Operators note" were applied - no route (`/owner`, `/owner/subscriptions`), role (`admin`), or identifier (`OwnerDashboard`, `owner/dashboard.tsx`, etc.) renamed, since none of those are user-visible. Every user-facing occurrence of the string across the Master Console's own header (`pages/owner/dashboard.tsx`, `pages/owner/subscriptions.tsx`), the Preview banner and its "back to console" link (`layout.tsx`), the CPO canvas's own preview-mode banner (`dashboard.tsx`), Quick Access (`role-select.tsx`), and the register-as-Owner confirmation copy (`register.tsx`) now reads "Master Console" - plus the internal code comments that used the same phrase, for consistency. A prior alternative, "Master Command Desk," was considered and rejected (confirmed via AskUserQuestion) - it would collide with "Command Desk," the existing name for the separate subscriber-facing Management app that this console can preview into, and having both in the product risked confusing which one a conversation meant.
- **Subscriptions became a full page with per-role pricing and a price-change history log** (supersedes the "Owner-editable subscription pricing" modal note above, and the single shared `pricePerAdditionalSeat` referenced in the three seat-management notes above it): per direct product direction ("this will affect every user, so we need to be very detailed" + "I need to be able to set the price for them all individually" + "each user role get a price and that includes the cpo and solo operator"). Two changes, landed together. (1) **Per-role pricing** - `pricingConfigTable` (`lib/db/src/schema/pricing-config.ts`) dropped the single `pricePerAdditionalSeat` column for five: `pricePerManagerSeat`/`pricePerOperationsSeat`/`pricePerFinanceSeat`/`pricePerHumanResourcesSeat`/`pricePerCpoSeat`, all still defaulting to $40 (matching the old shared default so the migration didn't silently change anyone's estimate), alongside the pre-existing `baseMonthlyPrice`/`soloOperatorMonthlyPrice` - 7 independently-settable prices total. `routes/companies.ts` gained `PRICE_FIELD_BY_ROLE` (`Record<ManagementRole, PricingField>`) and `CPO_PRICE_FIELD`, and `estimatedMonthlyCharge()` is now a per-role weighted sum instead of `additionalSeatsTotal * one price`. Every place a per-seat price surfaces now reads its own role's field: the Owner Console's company rows, and Command Desk's `GET`/`PATCH /users/seats` (`routes/users.ts`'s `buildSeats()`, via the already-exported `getOrCreatePricingConfig`) - `pricePerSeat` now rides on each role's own `CompanySeatUsage` object instead of one flat field on the response, so `AdditionalSeatsDialog`'s per-role price tags and running-cost total (`pages/admin/users.tsx`) each read their own role's number. (2) **Price-change history** - new `pricing_history` table (`lib/db/src/schema/pricing-history.ts`: `field`, `previousValue`, `newValue`, `percentageChange`, `changedAt`) logs every change to any of the 7 fields, regardless of which control made it. New `POST /companies/pricing/change` (replacing the old raw `PATCH /companies/pricing` setter) accepts `{field, newValue}` OR `{field, percentageChange}` (zod `.refine()` enforcing exactly one) and computes the other server-side - a percentage is never trusted pre-computed from the client, so the logged `percentageChange` is always consistent with the actual dollar change. `GET /companies/pricing/history` returns the full log, newest first. Both replace the old modal dialog with a real page, `/owner/subscriptions` (Owner-only, gated in `require-auth.tsx` the same way `/owner` itself is) - each of the 7 prices gets its own card with two independent controls ("Set current price" and "Increase by %", the latter showing a live preview of the resulting dollar amount before applying) plus a Price Change History table below (Field / Previous / New / Change % / Date, color-coded by direction). The Owner Console's "Subscription" stat tile is now a `<Link>` into this page instead of opening a dialog.
- **Live currency conversion on Command Desk's seat prices**: per direct product direction ("the subscriptions are all in dollars, we need a real engine for this because depending on which country you are it must be in that country currency" + "this engine must pull accurate data on exchange rates"). Scoped via AskUserQuestion to exactly this: Master Console (`/owner/subscriptions`, `routes/companies.ts`) stays USD-only - that's the one canonical source of truth the Owner sets prices in - and conversion only ever happens outward, on the subscriber-facing view (`AdditionalSeatsDialog`, `pages/admin/users.tsx`). New `artifacts/api-server/src/lib/currency.ts`: a company's currency is derived from its own offices (confirmed choice - `offices.country` is freeform text a Manager types in, not a picker, so a company has no dedicated currency/country field of its own), specifically the earliest-created office (`orderBy(createdAt asc) limit 1`) - a `COUNTRY_TO_CURRENCY` map (normalize-then-lookup, same strategy `lib/travel-advisory.ts` already uses for the identical "freeform country name never matches a canonical list by string equality" problem) resolves that to an ISO 4217 code. Live rates come from Frankfurter (`api.frankfurter.app`) - free, no API key, official ECB daily reference rates - chosen over a paid provider per direct product direction to avoid a new recurring cost before there's real subscription revenue (same reasoning already applied to deferring Ask Intelligence); rates are cached in-memory for 24h (`RATE_CACHE`) since they move at most daily, and a live-fetch failure falls back to the last known-good cached rate, or `{code: "USD", rate: 1}` if there's no cache yet - never a broken request. Only ECB-covered currencies (`SUPPORTED_CURRENCY_CODES`, ~31 codes) actually convert; anything else (most of Africa/Middle East/South America beyond Brazil/Mexico/South Africa) displays USD, a deliberate degradation given the free data source's real coverage limits. `GET`/`PATCH /users/seats` now also return `currency: {code, rate}`; `AdditionalSeatsDialog` formats every price (`Intl.NumberFormat` with `style: "currency"`) through it, with a "Prices converted to X at today's exchange rate" note whenever it isn't USD. **Not live-tested end-to-end** - this session's own sandbox network policy blocks outbound calls to api.frankfurter.app (confirmed via a direct `curl`, 403 from the sandbox's own proxy), so the actual fetch path is unverified here; the fallback-to-cached/USD behavior above exists specifically so a similar restriction on the real deployment target degrades gracefully instead of breaking the dialog - worth confirming live once deployed.
- **Master Console gets its own working-currency selector, tied into the same engine**: per direct product direction, following up on the note above - the Owner wanted to view/set the 7 canonical prices in a currency other than USD too (confirmed via AskUserQuestion: types AND displays converted, not display-only). A `Select` on `/owner/subscriptions` (`useWorkingCurrency`, persisted to `localStorage` same pattern as `office-scope.ts`) lets the Owner pick any of `SUPPORTED_CURRENCY_CODES`, backed by a new Owner-only `GET /companies/pricing/fx?currency=X` (`routes/companies.ts`, reuses `lib/currency.ts`'s exported `getExchangeRate` directly - the same rate cache/fallback the subscriber-facing engine uses, not a second implementation). Defaults to **ZAR, not USD** - per direct product direction ("this product will be only in SA until it begins to naturally spread to other countries"), that's the currency the Owner actually thinks in. Critically, **prices are still stored and logged in USD regardless of the selector** - `PriceFieldRow` converts what's *typed* into "Set current price" back to USD before calling `POST /companies/pricing/change` (`Math.round(typed / rate)`), and converts the API's USD response *outward* for display (`Math.round(value * rate)`) - the backend never receives or persists anything but USD, so `pricing_history`'s `previousValue`/`newValue` stay unambiguous no matter what currency the page was set to at the time; the Price Change History table itself is explicitly labeled "Always shown in USD" and never converts, precisely to keep that audit record legible regardless of the selector. "Increase by %" needs no conversion at all (a percentage of the USD value is the same percentage of the converted value) - only its preview figure is formatted through the selected currency. Initially distinct from the subscriber-facing engine in one key way, raised directly by the user when asked "do you remember we built in a location engine?" - Command Desk's conversion is fully automatic (derived from each company's own office), while this selector started manual-only, because the Owner has no "office" of their own in the data model for a location engine to derive a currency from. Closed that gap immediately after, when the user pointed out Operators Note already has its own real location engine (`resolveCurrentLocation` in `components/location-search.tsx` - browser geolocation + free Photon reverse geocoding, built for the CPO's "Current Area") that the Owner, as a person sitting at a browser, can use exactly the same way: a "use my location" button next to the selector calls that same function, then a new `GET /companies/pricing/currency-for-country` (also `routes/companies.ts`, wrapping `currencyForCountry` - now exported from `lib/currency.ts` rather than duplicated, so both engines resolve country names through the identical map) turns the detected country into a currency code and applies it. A manual override, not a replacement - the `Select` still works standalone, this is just a second, faster way to reach the same state.
- **Currency engine cut down to 5 currencies, not ~31**: per direct product direction ("this is becoming too complicated" followed by "in South Africa the option needs to be ZAR or USD, if the user is in Europe it needs to be in Euro, in the US it needs to be in USD, in China it needs to be Chinese Yuan, in England it needs to be British Pound, everywhere else it needs to be in USD") - a deliberate simplification, superseding the "~31 codes" figure in the two notes above. `SUPPORTED_CURRENCY_CODES` (both `lib/currency.ts` and its frontend mirror in `lib/api.ts`) is now exactly `["ZAR", "USD", "EUR", "GBP", "CNY"]`, and `COUNTRY_TO_CURRENCY` shrank to match - only South Africa (ZAR), the Eurozone countries (EUR), the UK/England (GBP), and China (CNY) are explicitly mapped; every other country (the US included) was deliberately removed from the map entirely rather than mapped to USD by hand, since `resolveCurrency`'s own existing fallback already returns USD for anything unmatched - "everywhere else is USD" was already built in, just needed the long tail of other-country entries removed to actually take effect. Same simplification closes the Master Console selector's scrolling problem for free - 5 options fit without ever needing the scroll workaround the previous commit added.
- **Quick Access "IT" tile built out: system status + a real support-ticket inbox**: per direct product direction ("this needs to monitor the website/App health, were logged tickets get send to all off it" + confirmed via AskUserQuestion this is for the Owner themselves, not a separate IT-staff role/account, and that a subscriber submits tickets which land in one shared Owner-visible inbox, no email delivery). New `/owner/it` page (Owner-only, gated/hidden-shell like `/owner/subscriptions`), replacing the old `href: null` placeholder. Two halves: (1) **System status** - `GET /system/status` (`routes/system.ts`, Owner-only) reports what's actually checkable today (a live `select 1` DB check, process uptime, Node version, environment, server time) - deliberately modest since real monitoring (Sentry etc.) is still Outstanding, not a substitute for it. (2) **Support tickets** - new `support_tickets` table (`lib/db/src/schema/support-tickets.ts`: companyId, userId, subject, description, `source` ("command_desk" | "operators_note"), `status` (open/in_progress/resolved/closed), `priority` (low/normal/high), `resolvedAt` stamped on the real transition into resolved/closed) closes the "support channel for subscribers" Outstanding item. `POST /support-tickets` (`routes/support-tickets.ts`) is the real intake - any authenticated company-scoped user, not Owner-restricted, added to `blockSoloOperatorFromManagement`'s `CPO_SURFACE_PATH_PREFIXES` allowlist so a Solo Operator CPO can submit too; `GET`/`PATCH /support-tickets` are Owner-only, deliberately cross-company (same aggregate-only-surface exception as `/companies`, except here it's full ticket content since reading and acting on it is the point). New shared `<ReportIssueDialog>` (`components/report-issue-dialog.tsx`) wired into both apps' own menus - a "Report an Issue" item in Command Desk's sidebar (`layout.tsx`, next to the Sign Out block) and Operators Note's operator menu (`dashboard.tsx`'s `TopBanner`, next to Profile) - each passing its own `source` so the Owner's inbox shows which surface a ticket came from. "Single Operator" tile also un-placeholdered in the same pass (confirmed via AskUserQuestion this meant the already-built Solo Operator plan, not the separate not-yet-built standalone downloadable app) - now links to `/cpo` (`requiresPreview: true`), since a Solo Operator company already redirects there automatically (`require-auth.tsx`), so no new backend work was needed, just wiring the tile up and updating its copy to remind the Owner to set their Test Company to that plan first. "Enterprise" was the one remaining placeholder at the time - removed outright in a follow-up pass, per direct product direction ("we have no need for it anymore"), superseding the "left as-is" note above.
- **Self-serve company registration** (supersedes the "No self-serve signup path yet" line in the Solo Operator plan note above): `/register` now covers both plans, not just Team - per direct product direction ("lets go to solo operator page" -> confirmed via a follow-up question that this meant building self-serve signup for it). `POST /auth/register`'s `RegisterSchema` (`routes/auth.ts`) gained `planType` (`"team"` default) and made `companyName` conditionally required via a `.refine()` (required for Team, ignored for Solo Operator) - the "exactly one CPO" hard cap on `POST /users` can never trigger here since this is always a brand-new company with no existing users; the backend creates the first user as `role: "cpo"` instead of `"manager"` for Solo Operator. Post-registration routing now branches on the created user's own `planType` (`/cpo` vs `/admin`) instead of always landing on `/admin` and relying on `require-auth.tsx`'s redirect to correct it after the fact - `lib/auth.tsx`'s `register()` also had a stale `tier?: "enterprise" | "micro_enterprise"` leftover in its inline type (mismatched against the `AuthContextValue` interface's own, already-correct declaration) cleaned up in the same pass.
- **`/register` redesigned as a two-step flow with its own plan-choice screen**: per direct product direction ("when you click register it needs to give me the option to select 'Solo Operator' or 'Management system'") - supersedes the first version's inline Plan `<Select>` dropdown inside the form. Clicking "Register" from the landing page now lands on a plan-choice screen first (two large clickable cards, not a dropdown) before the actual signup form ever appears; picking one reveals the form scoped to that plan (Solo Operator hides Company Name entirely, same as before), with a "← [Plan Name]" back link to return to the choice screen. **"Management system" is a display-label-only name for this one screen** - the internal `planType` value stays `"team"`, matching how "Command Desk"/"Operators note" are also just display labels layered over unchanged identifiers elsewhere in this app; every other surface (Owner Console, CLAUDE.md itself) still calls it "Team".
- **`/register` (Team) gains Position and an optional Office Location**: per direct product direction. (1) **Position** - a dropdown of the same four Management roles Command Desk's own "Add User" dialog offers (Manager/Finance/Human Resources/Operations), replacing the old hardcoded "the first user is always role: manager" behavior. `POST /auth/register`'s `RegisterSchema` gained `role` (enum of the four, default `"manager"`), ignored entirely for Solo Operator (that account is always `role: "cpo"`). (2) **Office Location** - just City + Country (confirmed via AskUserQuestion: optional, not required, and only those two fields rather than a full office form) - if both are filled in, the backend inserts a real `offices` row (`name` auto-derived as `"{city} HQ"`, matching the placeholder convention already on `pages/admin/offices.tsx`; `managerId` set to the newly-created user) right after creating the company and user; left blank, no office is created and the company falls back to Command Desk's existing "add an office later" flow. This isn't just record-keeping - it's a real signal for the currency engine (`lib/currency.ts`'s `resolveCurrency` derives a company's currency from its *earliest* office), so a Team company filling this in at signup gets accurate local pricing in Command Desk's Additional Seats dialog from day one instead of starting on USD until someone adds an office by hand.
- **Real Finance dashboard - first role-scoped Command Desk view** (supersedes the "just extra entry points into the one existing dashboard" line in the "Role Select landing page" note above): per direct product direction, confirmed via AskUserQuestion to be exactly the "Finance only seeing Quotations/Invoices/Payroll" shape that note had already flagged. New `/admin/finance` (`pages/admin/finance.tsx`) - three `SectionCard`s (Quotations/Invoices/Payroll, same visual pattern as the Management Dashboard's own `SectionCard`/`StatTile`), each a quick-glance summary (counts by status, outstanding/approved/paid totals, pending payroll + recent pay runs) with a "View all →" link into the real, full page for that module (`/admin/costs`, `/admin/invoices`, `/admin/payroll`) - this page surfaces the numbers, it doesn't duplicate those pages' actual workflows. Office-scoped like every other Command Desk list page (`filterByOffice` on Quotes/Invoices; Payroll has no `officeId` so isn't filtered). Also a new sidebar nav item ("Finance", between Clients and Quotations). **Confirmed via AskUserQuestion this is now a genuine role-scoped destination, not just a Quick Access preview** - `role: "finance"` sessions land here automatically after login/registration instead of the general Management Dashboard (`lib/auth.tsx`'s `login()`/`register()`, `require-auth.tsx`'s `homeRoute`) - the first time any Management-side role has its own distinct landing page rather than all sharing `/admin`. Not a permission boundary - a Finance user can still navigate anywhere else in Command Desk, same looseness every other role already has; this only changes where they land.
- **`/register` shows the subscription price with a required agreement checkbox, plus a payment-details slide-out**: per direct product direction, confirmed via AskUserQuestion - checkbox required to check before the Register/Create Account button enables (not just informational), applies to both plans (each shows its own flat base price). New unauthenticated `GET /auth/pricing` (`routes/auth.ts`, reusing `getOrCreatePricingConfig` exported from `companies.ts`) returns just the two flat base figures (`baseMonthlyPrice`, `soloOperatorMonthlyPrice`) - no per-seat prices or history, since seats aren't collected at signup and this route has to work before any session/company exists, unlike the rest of the Owner-only `/companies/pricing` surface. Always USD - no office exists yet at this point for the currency engine to derive a local currency from. Checking the box also slides a right-hand panel out (custom-built to match this page's own dark styling, not the standard shadcn `Sheet` which assumes the light `background`/`foreground` tokens the rest of Command Desk uses) with card-detail-shaped fields (Name on Card, Card Number, Expiry, CVC) and a "Save Card - $X/mo" button. **Confirmed via AskUserQuestion this is a UI stub, not a real payment integration** - same non-functional-but-honest pattern as Command Desk's own seat-purchase "Buy" button (`AdditionalSeatsDialog`, see the seat-cost notes above): the amber "No payment processor is connected yet - Save Card applies no actual charge" disclaimer mirrors that button's own wording, and clicking it only sets local `cardSaved` state (a "Payment method saved" toast/inline confirmation) - no card data is ever sent to the backend or stored anywhere, since VenueGuard must never collect raw card numbers into its own form fields (a real integration needs a processor's own hosted fields, e.g. Stripe Elements, to stay PCI-compliant). **Confirmed as a standing product requirement, not just this stub's own scope**: VenueGuard must never retain card details at all - every purchase (this signup flow, and eventually Command Desk's own seat-purchase "Buy" flow once real billing exists) re-collects card details each time rather than keeping one on file. A real payment integration is still its own Outstanding item below, but whatever processor eventually gets wired in must be used in a mode that requires re-entry per charge (or otherwise never lets VenueGuard's own database hold a stored payment method) - not a "card on file" model.
- **`/register`'s payment panel upgraded from a UI stub to a real, connect-later Stripe integration**: per direct product direction ("build the real capability now and connect it late[r]... same as we did previously we built it and then later we connect everything together") - same pattern already used for the currency engine's Frankfurter API (built and correct, live-tested only once deployed somewhere the sandbox's own network policy doesn't block). New `artifacts/api-server/src/lib/stripe.ts`: `isStripeConfigured()`/`getStripePublishableKey()` read `STRIPE_SECRET_KEY`/`STRIPE_PUBLISHABLE_KEY` from the environment at call time (deliberately *not* a fail-fast startup check like `DATABASE_URL`/`SESSION_SECRET` in `lib/db/src/index.ts` - going live with real billing is a separate, later decision), and `createCardValidationSetupIntent()`/`verifySetupIntentSucceeded()` wrap the Stripe SDK. Confirmed via AskUserQuestion: validate-only, no charge at signup (companies still start on `trial` status, unbilled - a real charge is a separate future decision), and scoped to `/register` only for now (Command Desk's seat-purchase "Buy" button keeps its existing free stub). New unauthenticated `GET /auth/stripe/config` (`{enabled, publishableKey}`) lets `/register` know at runtime whether a real account is connected, and `POST /auth/stripe/setup-intent` creates the SetupIntent the frontend confirms - **no Stripe Customer is ever created and the resulting PaymentMethod is never attached to anything**, which is what actually delivers the "never keep card on file" rule above: once the browser round-trip finishes, nothing about the card exists anywhere except transiently inside Stripe's own systems. `usage: "on_session"` (not the API default `"off_session"`) reflects that honestly - this PaymentMethod is never intended to be reused. `RegisterSchema` gained an optional `stripeSetupIntentId`, verified server-side (`verifySetupIntentSucceeded`) rather than trusted as a client claim before an account is created when Stripe is connected - matching this route's existing `callerIsOwner` pattern of never trusting a client-supplied security-relevant flag. Frontend: `register.tsx` split the payment panel into `StripeCardForm` (real - `@stripe/react-stripe-js`'s split `CardNumberElement`/`CardExpiryElement`/`CardCvcElement`, styled via Stripe's own `style` API since Elements render into an iframe Tailwind classes can't reach) and `StubCardForm` (the original non-functional stub, kept verbatim as the fallback), switched on `GET /auth/stripe/config`'s live `enabled` flag - **zero frontend redeploy needed once real keys are set on the backend**. `canSubmit` requires `cardSaved` whenever the signup path needs a card (see the two-path note below for how that's now decided). Smoke-tested in this sandbox with a temporary local `SESSION_SECRET` (no live DB, matching this environment's standing limitation) - confirmed `GET /auth/stripe/config` returns `{enabled: false, publishableKey: null}` and `POST /auth/stripe/setup-intent` correctly 503s when unconfigured, exactly as designed; the real Stripe-connected path itself is unverified here for the same reason the currency engine's live fetch was - no real Stripe account/keys exist in this environment. New dependencies: `stripe` (backend), `@stripe/stripe-js` + `@stripe/react-stripe-js` (frontend) - all pure JS, no native build risk, matching this repo's existing dependency profile.
- **`/register`'s single "I agree" checkbox split into two exclusive signup paths, one of them card-free**: per direct product direction - the single checkbox from the note above is now two: "I already know VenueGuard - subscribe now at $X/month" (opens the payment panel exactly as before) and "Start 14-day free trial - no card required" (skips the panel entirely). Picking one clears the other's state (`register.tsx`'s `selectSignupPath`); re-picking the already-selected option deselects it. This followed directly from a design conversation about whether a trial should require a card upfront at all - landed on no, since VenueGuard has no automatic billing yet (trial→active is still a manual Owner flip on `/owner`, and nothing charges even with Stripe connected), so requiring a card for a guarantee the system can't back up was pure friction; the earlier "must validate a card to register" behavior only made sense before this split existed. `canSubmit`'s card requirement is now keyed off `signupPath === "subscribe"` rather than off whether Stripe itself is connected - a trial signup never needs `cardSaved`, subscribe-now always does (via the real Stripe form or, until Stripe is connected, the same stub as before). **Backend fix that came out of this**: `POST /auth/register`'s card-required check used to be gated on `isStripeConfigured()` alone (an artifact of the old single-path design, where a card was always collected once Stripe was live) - now a trial signup with Stripe connected and no `stripeSetupIntentId` would have been wrongly rejected. Changed to only verify a SetupIntent when one is actually supplied (`if (parsed.data.stripeSetupIntentId && !(await verifySetupIntentSucceeded(...)))`), regardless of whether Stripe is configured - correct for all three real cases: Stripe not connected (never sent), trial chosen (deliberately not sent), subscribe-now chosen (sent and verified).
- **`trialEndsAt` - the 14-day trial is now a real, dated field, surfaced on the Master Console**: per direct product direction, confirmed that self-serve registration already let both plans register on the trial path with no card and that the trial genuinely starts the instant the company row is created (`companies.createdAt` + `status: "trial"`, no separate activation step) - what was missing was anything tracking *when* the 14 days actually ends. New `companies.trial_ends_at` (nullable timestamp, `lib/db/src/schema/companies.ts`) - set once, at creation, by a new shared `trialEndsAtFor(status)` helper (`routes/companies.ts`, exported the same way `getOrCreatePricingConfig` already is for `routes/auth.ts` to reuse) that returns `createdAt + 14 days` when `status === "trial"`, `null` otherwise. Wired into both real company-creation paths so they share one "14 days" definition rather than two: `/auth/register` (always `status: "trial"` today, so always gets a `trialEndsAt`) and the Owner Console's own `POST /companies` (`NewCompanyDialog`, which also defaults new companies to `trial`). Surfaced on `/owner`'s company table - a small "Ends {date}" line under the Status dropdown, shown only while `status === "trial"` and `trialEndsAt` is set (naturally disappears once the Owner flips status to `active`/etc.). **Informational only, same as every other billing-adjacent field in this table** (`estimatedMonthlyCharge`, the seat counts) - nothing reads `trialEndsAt` to actually expire or auto-suspend a company yet; that's real trial-enforcement logic and stays a separate, not-yet-decided Outstanding item, same as billing-cycle-aware seat purchasing above.
- **Real HR and Operations dashboards - same pattern extended to the other two Management roles**: per direct product direction ("Good now lets do same for HR and Operations"), following straight on from the Finance dashboard above. Scoped via AskUserQuestion: HR = Operator Database + Users; Operations = Tasks + Operator Deployment + Schedule. New `/admin/hr` (`pages/admin/hr.tsx`) - two `SectionCard`s, Operator Database (Pending/Onboarded/Denied counts from `operator_onboarding`, unfiltered by office - that entity isn't office-scoped anywhere else either) and Users (active headcount by the four Management roles, office-scoped via `filterByOffice`), each a "View all →" into the real `/admin/onboarding`/`/admin/users` pages. New `/admin/operations` (`pages/admin/operations.tsx`) - three `SectionCard`s, Tasks (by status + unassigned count), Operator Deployment (operators currently deployed, active task count), Schedule (tasks due in the next 7 days), all office-scoped, linking out to `/tasks`/`/admin/cpo-deployment`/`/admin/schedule`. Both follow the exact same summary-only, link-out-for-the-real-workflow shape as Finance's dashboard - new sidebar nav items ("Human Resources", "Operations", between Finance and Quotations) - and `role: "human_resources"`/`role: "operations"` sessions now land on their own dashboard after login/registration instead of the general Management Dashboard, same as Finance. The three now-duplicated role→home-route ternaries (`require-auth.tsx`'s `homeRoute`, `lib/auth.tsx`'s `login()` and `register()`) were consolidated into one shared `MANAGEMENT_HOME_ROUTE: Partial<Record<UserRole, string>>` lookup (`lib/api.ts`) rather than continuing to extend three separate hand-written ternary chains a third time; `register.tsx`'s own fallback redirect uses it too. Quick Access (`role-select.tsx`)'s Operations and Human Resources tiles now point at their real dashboards (`/admin/operations`, `/admin/hr`) with matching descriptions, instead of both falling through to the general `/admin` tile like Finance's used to before its own fix.
- **Live check-in / panic-alert signal - Following Roadmap Tier 1, item 1, first item built from that backlog**: per direct product direction, scoped via AskUserQuestion - both a scheduled "ok" check-in and a manual panic trigger (not one or the other), location captured on every trigger, and visible to every Management-side user (not narrowed by role). New `checkins` table (`lib/db/src/schema/checkins.ts`: `taskId`, `cpoId`, `type` (`"ok" | "panic" | "missed"`), `latitude`/`longitude`/`locationLabel`, `acknowledgedBy`/`acknowledgedAt`) plus an opt-in `tasks.checkInIntervalMinutes` (nullable - only tasks a Manager explicitly turns this on for expect scheduled check-ins; Panic itself is never gated on this, since an emergency isn't tied to a schedule). New `POST`/`GET /checkins` (`routes/checkins.ts`) - `POST` validates the CPO is actually on the task's roster (`task_assignments`) before accepting, cpoId always from the session; `PATCH /checkins/:id` acknowledges, `acknowledgedBy` likewise always from the session. New `lib/checkin-monitor.ts` - a background scan for overdue scheduled check-ins, same `setInterval`-on-boot pattern as `lib/gdelt-monitor.ts` (5-minute cycle vs. GDELT's 15), started alongside it in `index.ts`; raises a `"missed"` row once per overdue gap (checked by whether the CPO's most recent row is already `"missed"`, so it doesn't spam every scan cycle) - a fresh "ok" check-in or an acknowledgment is what resets the clock. `/checkins` added to `blockSoloOperatorFromManagement`'s `CPO_SURFACE_PATH_PREFIXES` so a Solo Operator CPO can use it too, though there's a known gap there - a Solo Operator company has no Manager account at all, so nobody currently sees that plan's own panic/missed alerts; flagged, not solved, since a Solo Operator's duty-of-care model is a separate design question. Frontend: Operators Note (`pages/dashboard.tsx`) gets Check In / Panic buttons on each `in_progress` task row in the Tasks panel (Panic behind a `window.confirm` to avoid an accidental tap) - location comes from the existing `resolveCurrentLocation()` (`components/location-search.tsx`, same engine as "Current Area" and the currency detector), best-effort only, a denied/unavailable geolocation permission doesn't block sending the signal itself. This file has no react-query/toast usage anywhere in it (a different pattern than the rest of the app), so the new code matches its own existing plain-promise-plus-`useState` convention (see `submitPlan`) rather than introducing one. Command Desk gets a new "Safety Alerts" panel at the very top of the existing `/alerts` page (`pages/alerts/list.tsx`, above the pre-existing `TaskFlagsPanel`/OSINT alerts list) - `/alerts` was already the one shared, role-agnostic Command Desk destination reachable from the sidebar regardless of which dashboard a role lands on, so it was the natural home rather than picking one of Finance/HR/Operations/Manager's four separate dashboards. Only unacknowledged panic/missed rows show (an "ok" check-in never surfaces here) with an Acknowledge action. Managers set the check-in interval per task via `EditTaskDialog` on `/tasks` (`pages/tasks/list.tsx`) - a "Check-In Interval (minutes)" field, blank by default (deliberately edit-only, not on the initial creation intake form, to keep that form from growing further). Smoke-tested in this sandbox (no live DB): confirmed the server boots with the check-in monitor running alongside the GDELT one, and that it fails the same graceful/logged way GDELT already does with no DB to query.
- **Job Profitability per task - Following Roadmap Tier 1, item 2**: per direct product direction - revenue next to cost, per task, so a job quietly losing money surfaces immediately instead of being discovered in hindsight. Extends the Quotations page's existing "Total Cost by Task" table (`pages/admin/costs.tsx` - the file behind the `/admin/costs` route, which despite its filename renders as "Quotations"; already had Personnel Cost + Expenses combined per task) rather than building a separate page, since all the underlying data (`quotes`, a newly-added `invoices` fetch, `personnelLines`, `expenses`) was already available or one query away. Revenue per task prefers an invoice's `totalAmount` over a quote's `totalQuoteValue` when both exist - an invoice is the more truthful "what this job actually earned" once one's been raised, since it can include added costs beyond the original quote (see the Invoices note above). No new backend endpoint - computed entirely client-side by joining the office-scoped `quotes`/`invoices`/`personnelLines`/`expenses` already fetched on this page, same pattern the existing cost table already used. The renamed "Job Profitability" table sorts worst-margin-first (not highest-cost-first like the table it replaced) so an underwater job is the first thing a Manager sees, not something they'd have to go looking for. **Currency-naive, same documented limitation the table it replaced already carried**: personnel cost has no currency of its own (matching the rest of this app's Personnel Cost convention) and margin is computed as a flat `revenue - cost` subtraction - only meaningful when everything for that task is actually in one currency, which this doesn't verify; the table's own caption says so.
- **Aging Receivables - Following Roadmap Tier 1, item 3**: per direct product direction - a sent invoice past its own due date, flagged before it becomes a payroll problem. No schema/backend change - `invoices.status`/`dueDate` already existed, this is purely a new computed view (`daysOverdue()`, `pages/admin/invoices.tsx`: `status === "sent"` and `dueDate` in the past). New "Aging Receivables" card on `/admin/invoices` (red-bordered, above the main invoice table, same visual treatment as the Safety Alerts panel's urgency) lists every overdue invoice sorted most-days-overdue-first, each clickable straight into `InvoiceDialog` to act on it; the main invoice table's own Due column also gets a small red "N days overdue" line so it's visible list-wide, not just in the dedicated card. Finance dashboard (`/admin/finance`) gets two new red-toned stat tiles on the existing Invoices `SectionCard` - Overdue count and Overdue value - so a Finance user sees it without navigating into Invoices at all; `StatTile` there gained an optional `tone="negative"` prop for this (first tone variant on that page's own local `StatTile`, mirroring the `tone` prop the Quotations page's own `StatTile` already had).
- **Certification/license expiry tracking - Following Roadmap Tier 1, item 4**: per direct product direction - a lapsed PSIRA/SIA/etc. registration is a liability and contract-losing event, easy to miss buried in one operator's own file. Turned out to be mostly a missing *view*, not a missing data model - `operator_documents` already had `expiryDate`/`documentType`/`verified` columns from the original onboarding-documents build, just no company-wide way to see across them. Two real gaps closed: (1) `DOCUMENT_TYPES` (`lib/onboarding-checklist.ts`, mirrored in `pages/admin/onboarding.tsx` and `lib/api.ts`'s `DocumentType`) grew from just ID Document/Passport to include the actual jurisdiction-specific cert types CPOs carry - PSIRA Registration, SIA License, Firearm Competency Certificate, Medical/First Aid Certificate, Driver's License, Other Certification/License - so there's something meaningful to attach an expiry date to in the first place. (2) New `GET /onboarding-documents` (`routes/onboarding.ts`) - every existing document route was scoped to one operator's own onboarding record; this is the one company-wide list, joined with each document's operator name, that a "see expiry across the whole roster" view needs. New "Expiring Certifications" card on `/admin/onboarding` (same red-bordered urgency treatment as Aging Receivables/Safety Alerts) - a fixed 30-day heads-up window (`EXPIRY_WARNING_DAYS`, not a company setting, matching how other Following Roadmap items shipped without new config surfaces), sorted most-urgent-first (already-expired sorts ahead of still-inside-the-window). HR dashboard (`/admin/hr`) gets matching "Certs expiring"/"Certs expired" red-toned stat tiles on the Operator Database `SectionCard`, same `tone="negative"` `StatTile` pattern as Finance's Overdue tiles - the expiry-math helper is duplicated (not shared) between the two pages, matching this codebase's existing convention for small page-local helpers like `formatMoney`.
- **One-tap emergency info - Following Roadmap Tier 1, item 5, completing all of Tier 1**: per direct product direction - nearest hospital/police station/embassy for wherever a CPO actually is right now, one tap away during an active task. Built on top of an existing, already-working library rather than a new integration - `lib/nearby-services.ts`'s `fetchNearbyServices()` (Overpass API, free/no-key OSM data, same source Photon/search and OSRM/routing already use) already found hospitals/police stations along a *route corridor* for Route Planning's "Calculate Route"; this adds a sibling `fetchNearbyEmergencyInfo(lat, lng)` that searches a 20km radius around a single point instead (wider than the route corridor's 5km buffer, since there's no route to stay "along," and embassies especially can be sparse), and adds embassies via OSM's `diplomatic=embassy` tagging - the one category the route-corridor version doesn't cover. New `GET /emergency-info?lat=&lng=` (`routes/emergency-info.ts`), same "not tied to a Venue record" shape as the existing `GET /weather`/`GET /traffic`, added to `blockSoloOperatorFromManagement`'s CPO allowlist. Frontend: a "Nearby Help" button sits right below Check In/Panic on each `in_progress` task row in Operators Note's Tasks panel (`pages/dashboard.tsx`) - tapping it always does a fresh lookup (never cached, since the point is "where am I right now") via the same `resolveCurrentLocation()` engine already reused for check-ins/Current Area/currency detection, then shows the single nearest Hospital/Police/Embassy each with distance and a "Directions" link straight to Google Maps (`https://www.google.com/maps/dir/?api=1&destination=lat,lng` - a plain external link, no API key needed). Not live-tested end-to-end in this sandbox for the same reason the currency engine's Frankfurter calls weren't - real network access wasn't verified here, but the underlying Overpass integration is the same one `fetchNearbyServices()` already uses successfully for Route Planning.
- **A real, always-visible panic button on Operators Note**: per direct product direction ("let's put an actual panic button on operators note") - follow-up to Tier 1 item 1's check-in/panic signal, which had only lived buried inside a specific `in_progress` task's row in the Tasks panel. Confirmed via AskUserQuestion: works with no active task required, not just relocated - an emergency isn't only possible during a formally in_progress task. Required a real schema change: `checkins.taskId` (`lib/db/src/schema/checkins.ts`) went from `notNull()` to nullable, `onDelete` from `cascade` to `set null` so a safety record outlives whichever task it happened to be tied to (or lack of one) rather than being deleted alongside it. `POST /checkins`'s roster-membership validation (`routes/checkins.ts`) now only runs when a `taskId` is actually supplied - skipped entirely for the global button. New always-visible "Panic" button in `TopBanner` (`pages/dashboard.tsx`, styled as the loudest control in the header, next to the Alerts trigger) - confirm-tap, then the same `resolveCurrentLocation()` capture and `POST /checkins` (`type: "panic"`, no `taskId`) as the per-task version, with inline loading/sent feedback on the button itself rather than a toast (matching this component's own established plain-state pattern - TopBanner and `OperationalCanvas` are siblings communicating via window events elsewhere in this file, so this button intentionally carries its own small, separate state rather than reaching into `OperationalCanvas`'s per-task `checkinState`). The original per-task Check In/Panic buttons in the Tasks panel are unchanged and still pass a real `taskId`. Command Desk's Safety Alerts panel (`pages/alerts/list.tsx`) already rendered `taskTitle` behind a null-safe conditional, so a task-less panic alert shows cleanly (operator name, no task line) with no frontend change needed there.
- **Offline-first sync for Timesheet + a new Field Incident Report entry - Following Roadmap Tier 2, item 6** (built earlier this session but never logged here until this housekeeping pass - see the Outstanding-list-audit note below for how that gap was found): new `lib/offline-queue.ts` (frontend) writes Operators Note submissions to `localStorage` before attempting the network call, so a CPO in a dead zone doesn't lose the entry - it retries automatically once back online (or on a periodic sweep), and a genuine server rejection (vs. a connectivity failure) surfaces as "failed" with manual retry/discard rather than looping forever. A sync-status indicator in `TopBanner` shows pending/syncing/failed items. Wired into two entry types: Timesheet entries (existing - saves optimistically into local state tagged `pendingSync`, reconciled via a real refetch once the queued upsert actually lands) and Field Incident Reports (new - a CPO's own first-hand field report, distinct from the OSINT-sourced Incidents entity). New `field_incident_reports` table/route (mirrors `checkins`' shape: nullable `taskId`, roster check when one is set, `cpoId` always from the session), a "Report Incident" dialog in Operators Note, and a Field Incident Reports review panel on Command Desk's `/alerts` (mirrors the Safety Alerts panel). The sidebar's Alerts badge now also counts unreviewed reports alongside unacknowledged checkins.
- **Outstanding-list audit pass**: per direct product direction to keep working through the Outstanding/Roadmap backlog, started by actually re-reading the repo's git log against CLAUDE.md's own bookkeeping first rather than assuming the doc was current - found the offline-sync entry above had shipped (`eb8a4d5`) without ever getting a Notes entry or its Following Roadmap Tier 2 item struck through, so both are fixed in this pass. Also closed the one still-open "Known rough edges" item: `OPERATIONAL_ALERTS`'s frozen "2 hr ago" timestamp, flagged as a sweep candidate alongside the (already-fixed) hardcoded "Cape Town" alert - reviewed and confirmed **not** the same bug. `OPERATIONAL_ALERTS` is a deliberate placeholder feed (`dashboard.tsx`, same "disappears once real data exists" convention as `MOCK_TASK`) that only renders when `realAlerts` is empty, so its frozen timestamp is never actually shown alongside live data the way the old Cape Town alert was (that one rendered unconditionally). No code change needed there.
- **Database indexes on every `company_id` column** - closes the Outstanding item flagged when multi-tenancy first shipped ("added as a filter everywhere, but with no index yet"). All 36 tables carrying a `company_id` column now get a plain btree index on it (`idx_<table>_company_id`, via drizzle-orm's `index()` in each schema file's `extraConfig`) - `company_settings` was already covered by its own `.unique()` constraint on `company_id`, so 35 new indexes plus that one pre-existing one covers every table. The four tables whose `extraConfig` already declared a `unique()` constraint (`task_assignments`, `task_routes`, `timesheet_entries`, `venue_risk_assessments`) got that constraint explicitly named (matching its existing auto-generated DB name exactly) and converted from the older object-return `extraConfig` style to the array-return style already used everywhere else, for consistency - the object style still works in this drizzle-orm version, this was purely a style unification, not a functional fix. **Known drizzle-kit push behavior, not a bug**: on a table that already has a `unique()` constraint, `drizzle-kit push` conservatively re-confirms that constraint (even though it's unchanged) whenever *any* other `extraConfig` entry on that table changes - it prompts "Do you want to truncate this table?" for tables with existing rows, requiring a real terminal to answer. **Answer No** - the constraint is already satisfied by the existing data, so truncating would only lose real rows for no reason. Confirmed via a live scratch Postgres that the full push applies cleanly and all 36 indexes land correctly once that prompt (if it appears) is answered.
- **Security headers + rate limiting on login/register** - closes two Outstanding items together. `helmet` is now mounted first in `app.ts`'s middleware chain, with `contentSecurityPolicy` and `crossOriginEmbedderPolicy` both explicitly turned off - this app leans on a long list of third-party origins (Google Fonts, Stripe Elements, the weather/traffic/currency/nearby-services providers, Google Maps links) that a real CSP/COEP needs deliberate tuning against rather than a guess; everything else helmet sets by default (X-Frame-Options, X-Content-Type-Options, HSTS, Referrer-Policy, etc.) needed no app-specific tuning and is on. New `lib/rate-limit.ts` (`express-rate-limit`) exports `loginLimiter` (15 attempts / 15 min) and `registerLimiter` (5 / hour), both keyed by IP and applied only to `POST /auth/login`/`POST /auth/register` - the two endpoints with no other gate in front of them (`requireAuth` doesn't apply, they're registered before it). `app.ts` also gained `app.set("trust proxy", 1)`, trusting exactly one reverse-proxy hop - without it, `req.ip` (what the limiters key on) would resolve to the proxy's own address for every request once this is actually deployed behind one, collapsing every real client into a single shared bucket. Confirmed live: security headers present on every response and absent for CSP/COEP as intended, 16th login attempt in a window correctly 429s with the configured message, and the login/landing pages both render and function normally with helmet active (a batch of `ERR_CONNECTION_RESET`/`ERR_TUNNEL_CONNECTION_FAILED` console errors during that check were Google Fonts being blocked by this sandbox's own outbound network policy, not something the header change caused - same pre-existing sandbox limitation as the Frankfurter currency API's own unverified live call).
- **CI on every PR** - closes the Outstanding item; every commit was manually built/typechecked before this. New `.github/workflows/ci.yml`, one job on `pull_request`/`push` to `main`: checkout, `pnpm/action-setup` pinned to the same `11.9.0` this repo's own `packageManager` field specifies, `actions/setup-node` reading the Node version from the existing `.nvmrc` (not hand-duplicated), `pnpm install --frozen-lockfile`, then the exact `pnpm run build` the repo's own Workflow section already requires locally before every commit - so CI enforces the same bar, not a separate one. `PORT`/`BASE_PATH` are set to harmless placeholder values (the frontend build needs them per `vite.config.ts`, same as running the build locally) - no `DATABASE_URL`/`SESSION_SECRET`, since typechecking and bundling never executes the app or touches a database. Verified locally by running the identical `pnpm install --frozen-lockfile && pnpm run build` sequence against a clean checkout of `main` before writing the workflow, confirming it genuinely passes rather than assuming it would.
- **Seat-limit enforcement** - closes the Outstanding item; the base+additional seat counts have displayed since the seat model shipped, but nothing actually stopped a company from creating a user past its limit until now. New `checkSeatAvailable(companyId, role)` (`routes/companies.ts`, exported the same way `getOrCreatePricingConfig`/`trialEndsAtFor` already are for other routes to share) counts active users of that role against the company's base+additional limit and is the one shared rule both real user-creation paths now check before inserting: `POST /users` (`routes/users.ts`, for the four Management roles - CPO is deliberately excluded there since the only "cpo" role that route ever creates in practice is a brand-new Solo Operator's single hard-capped account, already covered by its own pre-existing check) and the onboarding operational-access grant (`routes/onboarding.ts`, for Team-company CPOs, checked against the separate CPO base+additional pool). Both return a 403 with a "buy additional seats" message on the used-count already-at-limit case; no frontend change needed since both call sites already toast `e.message` from a failed mutation. Deliberately simple - a hard block at exactly the limit, no soft-warning tier - since the seat model's own "Buy" flow already exists as the intended next step for a company that hits it.
- **Error tracking (Sentry) - built now, connected later** - closes half of the Operational safety net's "Error tracking / uptime monitoring" item (uptime monitoring is the still-open half - see the Outstanding list). Same connect-later pattern already used for Stripe (`lib/stripe.ts`) and the currency engine's Frankfurter API - the wiring is real and complete, but `SENTRY_DSN`/`VITE_SENTRY_DSN` aren't set anywhere in this environment yet, so it's a silent no-op until they are. Backend: new `lib/error-tracking.ts` (`initErrorTracking`/`captureError`, `@sentry/node`) - initialized in `index.ts` before `app.listen` (so it's armed before the background monitors start too, not just inside Express's own request cycle), wired into `app.ts`'s catch-all error handler and into `process.on("uncaughtException"/"unhandledRejection")` for the crash-outside-a-request case those monitors could otherwise hit silently. Frontend: `main.tsx` calls `Sentry.init()` (`@sentry/react`) gated on `import.meta.env.VITE_SENTRY_DSN`. **Real bundling bug found and fixed while smoke-testing this** (`node dist/index.mjs` with a real DATABASE_URL that just couldn't connect, same as this session's other backend smoke tests) - `@sentry/node` pulls in `@opentelemetry/api`/`core`/`instrumentation`/`sdk-trace-base` transitively, but `build.mjs`'s esbuild config already had `"@opentelemetry/*"` in its `external` list (added defensively, pre-emptively, before anything in this repo actually needed it) - that left bare `import ... from "@opentelemetry/api"` statements in the bundled `dist/index.mjs` with nothing to resolve them against at runtime (`dist/` has no `node_modules` of its own), crashing on boot with `ERR_MODULE_NOT_FOUND`. Fixed by removing `@opentelemetry/*` from that external list (confirmed via grep it's pure JS with no native/dynamic-path dependency forcing it out of the bundle the way its neighbors in that list need) so esbuild actually bundles it - but that then surfaced a second, subtler problem: drizzle-orm has an *optional* peer dependency on `@opentelemetry/api`, and pnpm was resolving two structurally-incompatible "flavors" of `drizzle-orm` across the workspace (one for `artifacts/api-server`, where `@opentelemetry/api` was now visible via `@sentry/node`, and a different one for `scripts`/`lib/db`, where it wasn't) - a private-field (`shouldInlineParams`) type mismatch between the two flavors broke `pnpm run typecheck` in both those other workspaces even though neither one touches Sentry. Fixed by adding `@opentelemetry/api@1.9.1` as an explicit direct dependency of both `lib/db` and `scripts` (matching the version `@sentry/node` already resolved) so pnpm converges on one consistent peer-resolved `drizzle-orm` everywhere. Confirmed via a direct `node dist/index.mjs` run (no live DB, same standing sandbox limitation as every other backend smoke test this session) that the server now boots cleanly both with and without a `SENTRY_DSN` set, and that `grep -c 'from "@opentelemetry'` against the built bundle is 0 (fully inlined, no leftover unresolved imports).
- **Real "forgot password" email flow - built now, connected later** - closes the Outstanding item. Same connect-later pattern as Stripe/error tracking: new `lib/email.ts` wraps generic SMTP (`nodemailer`, gated on `SMTP_HOST`/`SMTP_PORT`/`SMTP_USER`/`SMTP_PASS`/`SMTP_FROM`) rather than a specific provider's SDK, so switching providers later (SES, SendGrid's SMTP relay, Postmark, a real mail server) is just env vars, not code. New `password_reset_tokens` table (`lib/db/src/schema/password-reset-tokens.ts` - opaque random `id` like `sessions`, `userId`, `expiresAt`, nullable `usedAt` stamped once consumed so a link can't be replayed) plus `POST /auth/forgot-password`/`POST /auth/reset-password` (`routes/auth.ts`), both unauthenticated (registered ahead of the central `requireAuth` gate, same as login/register) and both behind a new dedicated `forgotPasswordLimiter` (`lib/rate-limit.ts`, 5/hour) rather than reusing `loginLimiter`/`registerLimiter` - this surface's own real brake, since the response is deliberately identical whether or not the email belongs to a real account (same anti-enumeration convention as `/auth/login`'s generic "Invalid email or password"). When SMTP isn't connected (this environment, today), the reset link is logged server-side instead of emailed - same standing workaround as every other admin-created account (an Owner/Manager manually generating a one-time password), just now self-service-shaped once real SMTP is wired up. Frontend: new `/forgot-password` (request a link) and `/reset-password` (consume the token + set a new password, reads `?token=` via wouter's `useSearch`) pages, styled to match `/login`'s existing full-bleed auth-page treatment (`components/layout.tsx`'s `hideShell` list, `require-auth.tsx`'s unauthenticated-allowed list) - a "Forgot password?" link added next to `/login`'s password field. **Verified end-to-end against a real, live scratch Postgres** (not just a boot smoke test) - `drizzle-kit push` applied the new table cleanly, then a real running server confirmed: requesting a reset for a real vs. a nonexistent email returns byte-identical 202 responses (only the real one logs a token, tied to the correct `userId`); an unknown token 400s; a too-short new password 400s via the existing zod schema; a valid token+password 204s and the user can immediately log in with the new password (`mustChangePassword` stays `false`, unaffected); and a direct DB check after the reset confirms `used_at` is stamped, which the route's own `!row || row.usedAt` check would reject on a replay (a live replay-over-HTTP check hit `forgotPasswordLimiter`'s own 5/hour budget first, confirming that limiter itself works, which is arguably the more important thing to have confirmed live).
- **Automated database backups + a genuinely tested restore process** - closes the Operational safety net item. Two new scripts (`scripts/src/backup-database.ts`/`restore-database.ts`, `pnpm --filter @workspace/scripts run backup-db`/`restore-db`) shell out to `pg_dump`/`pg_restore` rather than reimplementing dump/restore logic in JS - real, battle-tested tools, already present in this environment (confirmed via `which pg_dump` before writing either script). `backup-db` writes a timestamped `--format=custom` dump to `BACKUP_DIR` (default `./backups`) - compressed, and the only format `pg_restore` can do a selective/parallel restore from later if that's ever needed. `restore-db` is deliberately destructive-and-guarded: refuses to run without an explicit `--yes` flag (not a bare `y/n` prompt, easy to reflexively hit) once it's printed exactly which `DATABASE_URL` it's about to overwrite. **"Automated" is the backup script existing and working, not a scheduler this repo runs itself** - same shape as uptime monitoring's "the endpoint exists, pointing an external pinger at it is a deployment task" a few lines above: there's no always-on process in this codebase that would be the right place to own a cron loop for this (the existing background monitors live and die with the API server process, wrong lifetime for a backup that must survive a redeploy) - wiring a real scheduler (cron/systemd timer/a managed platform's scheduled-job feature) is deployment work for whenever a real production host exists. **Genuinely tested, not just written** - ran a real `backup-db` against the same live scratch Postgres the forgot-password work above was verified against, then `restore-db` (first confirmed it correctly refuses without `--yes`) into a brand-new empty database, and confirmed via direct SQL that all 41 tables and their real rows came back intact - including that session's own `manager@acme.test` password-reset row (`used_at` still stamped), proving a genuine full round-trip rather than assuming `pg_dump`/`pg_restore` would just work.
- **Structured after-action reports - Following Roadmap Tier 2, item 7** (first Tier 2 item, following the direct instruction to keep working through the backlog once Tier 1 and the Outstanding list were both clear): scoped via AskUserQuestion - standard close-protection AAR shape (summary, incidents encountered, route/schedule deviations, client feedback, recommendations, replacing today's single free-text requirements field for this purpose), reviewed on the Task itself in Command Desk, and submittable by a CPO any time during or after the task (not gated to "completed only"). New `after_action_reports` table (`lib/db/src/schema/after-action-reports.ts`) - unlike `checkins`/`field_incident_reports`, `taskId` is required and non-nullable (an AAR is inherently about a specific job) with `onDelete: cascade` (matching `expensesTable.taskId`, the closest existing "required, task-owned child record"); a CPO can file more than one against the same task (an interim note, then a final one), append-only like `field_incident_reports` rather than one upsert-per-task record. New `GET/POST /after-action-reports` + `PATCH /after-action-reports/:id` (`routes/after-action-reports.ts`) - task-scoped (`?taskId=`, not company-wide like `field-incident-reports`' own GET, since the Task detail panel this backs is always asking "what's filed against this job"), the roster-membership check always runs (unlike the optional-taskId routes, where it's conditional), `cpoId`/`reviewedBy` always from the session. Frontend: new `AfterActionReportDialog` (Operators Note, launched per-task-row like Report Incident but with the task already known so no task selector) submits through the same offline queue (`lib/offline-queue.ts`, new `"after_action_report"` kind) - shown on each `in_progress`/`completed` task row in the Tasks panel, next to Check In/Panic/Nearby Help. Command Desk: a new `AfterActionReportsPanel` on `/tasks`' own expandable-row pattern (`pages/tasks/list.tsx`, sibling to the existing `TaskHoursPanel`/"Hours logged" toggle, but its own independent "After-action reports" toggle available on `in_progress` tasks too, not just `completed`) lists every report filed against that task with a "Mark Reviewed" action. **Verified end-to-end against the same live scratch Postgres** used for the forgot-password/backup work - logged in as a real seeded CPO and Manager (not just booted the server): filed a real report against a task the CPO is actually on the roster for (201, correct `cpoId`/`taskTitle`); confirmed the roster check actually rejects a task the CPO isn't assigned to (403, tested by inserting a second, unassigned task directly via SQL); confirmed a nonexistent task 404s; fetched it back via the task-scoped GET; and reviewed it as the Manager (`PATCH`, `reviewedBy`/`reviewedAt` correctly populated) - full real create → reject → list → review round-trip, not just a typecheck.

# Outstanding / Roadmap

Everything flagged as still-needed during this session's "what would it take to actually go live" conversation, kept here as a running punch list rather than scattered across chat history. Tackle in whatever order makes sense - roughly sequenced below by what unblocks what, not by strict priority. Update this list as items get built (move them into "Notes & Follow-ups" above) or as priorities change - it isn't a spec to build from unprompted, same rule as the Product Vision section: confirm before starting a chunk of this.

**Product surfaces still missing:**
- Actual subscription charges + webhooks flipping `companies.status` (`trial`/`active`/`suspended`/`cancelled`) automatically instead of by hand - self-serve registration (see Notes above) creates a `trial` company but nothing ever moves it to `active` without the Owner doing it by hand on `/owner`. **Partially built**: Stripe itself is now really wired up (`/register`'s payment panel, `lib/stripe.ts` - see the Notes entry above), but only to validate a card at signup, not to charge it - `STRIPE_SECRET_KEY`/`STRIPE_PUBLISHABLE_KEY` also still need to actually be set somewhere for any of it to activate at all. Real subscription charging (and the webhooks to react to them) is still a separate, later decision - **Standing constraint, confirmed per direct product direction**: VenueGuard must never store card details on file - every purchase (signup, and eventually Command Desk's own seat-purchase "Buy" flow) re-collects card details each time rather than keeping a saved payment method, so this stays true once real charging is built too. **Flagged per direct product direction as the finale step before going live** - connecting the real `STRIPE_SECRET_KEY`/`STRIPE_PUBLISHABLE_KEY` to an actual Stripe account happens last, only once every other product surface, security/hardening item, and business/non-code item on this list is otherwise ready - not to be done opportunistically earlier just because the wiring exists.
- Billing-cycle-aware seat purchasing: per direct product direction, buying additional seats (Command Desk's "Buy" button - see the seat-cost notes above) should make the seats available immediately but only add their cost to the recurring bill starting the *next* full billing cycle, not the current one. Deliberately not built yet - there's no billing-cycle/invoice concept anywhere in the system to delay against (today's Est. Monthly Charge is a live snapshot recomputed from current seat counts, not a cycle-based figure), and this needs real payment integration underneath it to mean anything. Needs, at minimum: a billing-cycle anchor date per company, and a distinction between "seats purchased" (immediate) vs. "seats counted in this cycle's charge" (lagged).
- ~~Seat-limit enforcement~~ - **Built**, see the Notes & Follow-ups entry below. A company can no longer create a user (any of the four Management roles, or a CPO past its own separate limit) beyond its base+additional seat count.
- Ask Intelligence (CPO-side differentiator, deliberately deferred until there's cashflow for paid AI APIs) and Operational Route corridor analysis (the other CPO differentiator discussed, not yet built)
- Per-account Master Console permissions - `/owner/subscriptions` (the 7 platform-wide subscription prices) is confidential per direct product direction ("only me the owner and those i give access must be able to see the subscription prices"), and today's `role: "admin"` gate means *every* Owner-side account sees it, with no way to grant/restrict pricing visibility to specific individual accounts. Flagged, not built - revisit as its own task.

**Security/hardening before handling real customers' credentials:**
- ~~A real "forgot password" email flow~~ - **Built**, see the Notes & Follow-ups entry below - same connect-later pattern as Stripe/error tracking, real wiring but no SMTP credentials are set anywhere in this environment yet, so it degrades to the Owner/admin manually generating a one-time password (today's pre-existing workaround) until they are.

**Known rough edges / loose ends spotted in passing** (not a priority list, just a place to catch small latent bugs and stale demo content before they're forgotten - add to this rather than losing track of them mid-session):
- ~~Report Incident/Report Issue dialogs rendered white-on-white when opened from Operators Note~~ - **Fixed**, see the panic-button-era Notes entry area (no explicit text color on the card, inherited `text-white` from the dark app shell).
- ~~`companies.ts`/`system.ts`'s router-wide `requireRole("admin")` silently 403'd every route registered after them for non-Owner accounts~~ - **Fixed** - this had been breaking the live panic/check-in feature and support tickets for every real subscriber, not just Owner-only surfaces.
- ~~Operators Note's Active Alerts panel showed a hardcoded "Cape Town" demo alert regardless of the CPO's actual location~~ - **Fixed** - now stamped with `briefArea.area` (the CPO's real current area), same field the weather/road-closure alerts already use.
- ~~Worth a deliberate sweep for more of the same pattern (`OPERATIONAL_ALERTS`'s frozen "2 hr ago" timestamp was the flagged candidate)~~ - **Reviewed, not a bug**: `OPERATIONAL_ALERTS` (`dashboard.tsx`) is a deliberate placeholder feed, same "disappears once real data exists" convention as `MOCK_TASK` - it only renders when `realAlerts` is empty, so its frozen timestamp is never shown next to live data, unlike the "Cape Town" alert above (which rendered unconditionally regardless of real data). No fix needed.

**Operational safety net:**
- ~~Error tracking (e.g. Sentry)~~ - **Built**, see the Notes & Follow-ups entry below - same connect-later pattern as Stripe/the currency engine, real wiring but `SENTRY_DSN`/`VITE_SENTRY_DSN` aren't set anywhere in this environment yet. **Uptime monitoring is a separate, still-open half of this item** - the existing `GET /healthz` (`routes/health.ts`, already unauthenticated) is a ready target for an external ping service, but pointing one at it is a deployment/non-code task, not something to build in-repo.
- ~~Automated production database backups + a tested restore process~~ - **Scripts built and the restore process genuinely tested**, see the Notes & Follow-ups entry below. "Automated" still needs a scheduler (cron/systemd timer/a managed platform's scheduled-job feature) pointed at the backup script once a real production host exists - that wiring is a deployment decision, not something to build in this repo (same shape as uptime monitoring's "the endpoint exists, pointing a pinger at it is a deployment task").

**Business/non-code (not this repo's work, but blocking a real launch):**
- Registered business entity, Terms of Service, Privacy Policy
- Actual decided pricing for the base plan and additional seats - now Owner-editable (`/owner`'s "Subscription" button, see the pricing-config note above) rather than hardcoded, but the numbers themselves are still placeholders until real prices are decided
- Hosting + a domain, connected together - mechanically simple once the above is far enough along (see this session's own walkthrough for the how-to)

# Following Roadmap - Industry Feature Backlog

Comes after the Outstanding list above, not instead of it - the items above are launch blockers, this is what comes next once VenueGuard is live. Built from a deliberate exercise: imagining the product through the eyes of a former international CPO who now owns a CPO company, first as a field operator (Operators Note) then as a subscriber running the business (Command Desk), asking specifically what would make either of them feel "where has this been all my life" - not just "useful," but solving a real, remembered moment of fear, loss, or embarrassment they'd already lived through. Confirmed by direct product direction as the following roadmap. Same rule as every other section here: this documents direction, it isn't a spec to build from unprompted - confirm scope before starting any item.

**Scope: the Team plan's Operators Note (tied to a company subscription, as it exists today), not the standalone downloadable product** - per direct product direction, the standalone freelance-CPO app (flagged in the Product Vision section above as a separate, bigger, not-yet-built vision item) is explicitly deferred; every "Operators Note" item in this backlog is scoped to the in-app CPO experience a Team company's own operators already use.

**Tier 1 - build these first, biggest "finally" reaction:**
1. ~~Live check-in / panic-alert signal~~ - **Built**, see the Notes & Follow-ups entry above. Operators Note triggers it (scheduled check-in + a panic/man-down trigger), Command Desk receives and monitors it via the Safety Alerts panel on `/alerts`.
2. ~~Job profitability per task~~ - **Built**, see the Notes & Follow-ups entry above. Revenue (invoiced, or quoted if not yet invoiced) next to cost, per task, on the Quotations page's "Job Profitability" table.
3. ~~Aging receivables~~ - **Built**, see the Notes & Follow-ups entry above. Overdue invoices flagged on `/admin/invoices` (Aging Receivables card + per-row indicator) and `/admin/finance` (Overdue stat tiles), most-overdue-first.
4. ~~Certification/license expiry tracking per operator per jurisdiction~~ - **Built on the Command Desk/HR side**, see the Notes & Follow-ups entry above (`/admin/onboarding`'s Expiring Certifications card + `/admin/hr` stat tiles). The self-service cert wallet on Operators Note (a CPO seeing/renewing their own expiring certs, rather than waiting on HR to chase them) was NOT built in this pass - still open.
5. ~~One-tap emergency info~~ - **Built**, see the Notes & Follow-ups entry above. "Nearby Help" button on each in_progress task in Operators Note's Tasks panel - nearest hospital/police/embassy with directions, via a new point-based sibling to the existing route-corridor `fetchNearbyServices()`.

**All five Tier 1 items are now built.** Tier 2 starts at item 6 below.

**Tier 2 - strong value, real pain, less visceral:**
6. ~~Offline-first sync for field timesheet/incident entries on Operators Note~~ - **Built**, see the Notes & Follow-ups entry above. Queues Timesheet saves and a new Field Incident Report entry type in `localStorage` and syncs once back online, instead of losing data in a dead zone.
7. ~~Structured after-action report tied to a task~~ - **Built**, see the Notes & Follow-ups entry below. Standard close-protection AAR shape (summary, incidents encountered, route/schedule deviations, client feedback, recommendations) - Operators Note files it, Command Desk reviews it on the Task itself.
8. Real client/principal protection profile - medical info, known threats, routine, family - built in Command Desk's Clients CRM, surfaced to the assigned CPO during their task. Today's "requirements" field is billing-shaped, not protection-shaped.
9. Operator utilization over time (not just the current snapshot stat tile) - are CPOs sitting idle or is work being turned down.
10. One compliance/risk rollup - expiring certs, pending onboarding docs, unapproved timesheets, unreviewed incidents - instead of five separate pages.
11. Client revenue concentration view - how exposed the business is to one account.
12. Quote win rate as an actual trend (approved / sent over time), not just Sent vs. Pending counts.
13. Shift handover notes between CPOs on multi-day details - different from the existing one-way Manager -> CPO Communications.
14. Equipment/asset tracking - issued/returned per task, serial numbers, maintenance (Command Desk tracks, Operators Note gets an issued/returned checklist per task).
15. Team-lead/hierarchy designation on multi-operator task rosters - who's team lead, driver, advance, close protection (Command Desk assigns, Operators Note displays).

**Tier 3 - real, but bigger builds / further out:**
16. Travel/visa logistics module - visa requirements by nationality+destination, local embassy/fixer contacts (built Command Desk-side like Field Intelligence, surfaced to the traveling CPO's own Operational Brief).
17. Contract/retainer management with renewal dates - standing details and recurring revenue, distinct from one-off Tasks/Quotes.
18. Pipeline/forecasting from open quotes - projected future revenue for staffing and cash planning.
19. Post-task client satisfaction/feedback loop, closing the quality circle.
20. Vendor/subcontractor performance tracking over time (Vendors CRM exists; this adds a track record, not just a contact card).
21. Insurance/liability policy tracking - coverage and expiry per operator or per contract.
22. Re-vetting/background-check renewal cadence - vetting isn't one-and-done.
23. Passport/visa expiry tracking for internationally-deployed CPOs.
24. Weapons/firearms permit tracking where applicable, same treatment as certs.
25. Client portal - the client sees their own task status/invoices instead of chasing email.
26. E-signature on quotes, not just a status flip - more defensible if a dispute comes up.
27. Scope-creep alerts - flag when actual task cost is diverging from the quote while the job is still running, not after.
28. Rate benchmarking - is a given day/night rate competitive for the region and risk level.
29. Real notifications - SMS/push/email for panic alerts, overdue invoices, expiring certs - things that can't wait for someone to open the app.
30. Granular per-role permissions - today Finance/Operations/HR all have the same looseness on Command Desk; this locks that down as the company grows.
31. Subscriber's own data export - reduces lock-in fear, builds trust in the platform.
32. Multi-language UI, for companies running local staff who aren't English-first.
33. GPS breadcrumb trail during active tasks (Operators Note), pairs with item 1.
34. CPO self-service availability/time-off requests on Operators Note.
