Country Boundary QA Mode - summary report (Index 2.2)

Generation timestamp: 2026-07-07T14:14:57Z
Registry version: src/lib/country-registry.ts (744475 bytes), generated by
                  public/data/generate-country-registry.py (Index 2.1,
                  reprojected Index 2.2B)
Projection: Web Mercator, custom seam at 191.1deg (=-168.9deg), clamped to
            +-85.05112877980659deg lat, projected into the full square
            1000x1000 source-image space (matching world-map-v17.png's own
            2048x2048 square 1:1, no crop assumption baked in). Consuming
            SVG overlays use viewBox="0 0 1000 1000" preserveAspectRatio=
            "xMidYMid slice" - the SVG-native equivalent of the base
            image's object-fit:cover - so boundaries stay aligned with the
            coastline at any live viewport aspect ratio (fixes the Index
            2.2A misalignment, which was caused by a fixed 2:1 crop
            assumption that only held at exactly a 2:1 viewport).

Countries available for review: 235
Countries reviewed this session: 0
Countries passing: 0
Countries flagged: 0
Adjustment file entries (public/data/country-adjustments.json): 0

Notes:
- This is the initial rollout of the QA Mode itself - no manual review
  pass has been conducted yet. Every count above is 0 by design, not by
  omission: the tool exists and is ready to use, but reviewing all 235
  countries one at a time is a manual, ongoing task for whoever uses the
  panel, not something this ticket's implementation performs on its own.
- Adjustments recorded via the QA panel's "Flag for Review" button exist
  only in the current browser session's React state - a static frontend
  has no filesystem write access, so persisting a flag into
  country-adjustments.json requires a reviewer to copy the exact JSON the
  panel logs to the console (format: {"ISO3": {"status": "review-required",
  "notes": "..."}}) into that file by hand. This report reflects the
  committed file's state, not any single browser session's in-memory flags.
- Spot-checked during implementation (Australia, against the real
  coastline and against the old hand-typed test-zone hit-box) - the
  generated boundary visibly tracks the real coastline far more precisely
  than the old approximation, consistent with Index 2.1's own alignment
  verification.
