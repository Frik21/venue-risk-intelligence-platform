# Sub-Layer Indexing Convention

## Rule

The project tracks major build stages of the Operational Canvas as numbered
layers (Layer 1 through Layer 8). Within a single layer, many smaller changes
occur over time. This convention gives each of those granular changes a
stable, referenceable sub-layer index.

- Whenever the user says the exact phrase **"New rule line"**, the current
  layer's sub-counter increments by one.
- The first use while on Layer 1 becomes `1.1`, the next `1.2`, then `1.3`,
  and so on, chronologically.
- When work moves to Layer 2, the sub-counter resets, and the first
  "New rule line" on Layer 2 becomes `2.1`, then `2.2`, etc.
- This pattern continues for every subsequent layer (`3.1, 3.2 ... 4.1,
  4.2 ...`, etc).
- Numbering is **not retroactive** - it begins fresh from the point this
  convention was introduced. Nothing built before this point is renumbered.

## Index

| Sub-Layer Index | Description |
|---|---|
| 1.3 | City-lights night-map - `NightMapLayer` in `artifacts/risk-assessments/src/pages/dashboard.tsx`, tiles in `artifacts/risk-assessments/public/data/tiles/`, generated by `generate-night-map-tiles.py`. Real Natural Earth city-lights + country-borders data baked into a Web Mercator XYZ tile pyramid: lit tiles show on the default world view, swaps to a lights-free set once zoomed into a country (landmass/coastline stay visible, only the glow hides). Replaced a single 16383px static image after that caused multi-second browser decode stalls on every country selection - tiling keeps only the handful of tiles visible at the current zoom in play, so it stays fast regardless of total map resolution. PRs #16, #17. |
| 1.4 | Restored pre-1.3 continent colours and ocean gradient after regression introduced during 1.3 tile-generation work; confirmed 1.3 swap functionality unaffected. `LANDMASS_FILL_COLOR` and the ocean gradient stops in `generate-night-map-tiles.py`, both measured directly off the pre-1.3 source image (`earth-at-night.png`, commit `4ecafb6`) rather than approximated. |
| 1.5 | Stopped landmass/city-lights from repeating on wide viewports. `NightMapLayer` in `dashboard.tsx` sets `noWrap` on the lit/no-lights `TileLayer`s so exactly one real copy of the world ever renders; whatever's left on a wide viewport falls through to the ocean's own CSS gradient (`index.css`) instead of a duplicated continent. (A real gradient-tile bottom layer was tried first, but repeating the ocean's photographic gradient - Index 1.4, a vignette centred on one real spot on Earth - just produced a second, out-of-place bright spot; the CSS fallback's colour stops were retuned to the same measured gradient values so the two tones stay close.) |
| 1.6 | Restored real edge-fade for the map's west/east edges on wide viewports - 1.5's `noWrap` correctly stopped duplicate landmass, but left the map's own real edges (Alaska/Russia Far East) exposed against the flat CSS fallback with no coastline continuation, reading as land abruptly cut off. `computeNightMapEdgeFadeBounds` + two `ImageOverlay`s in `NightMapLayer` (`dashboard.tsx`, lit-view only), images generated by `generate-night-map-edge-fades.py` - anchored pixel-exact to the lit tile set's own west/east edge columns (`tiles/lit/4/0_*` and `15_*`), not invented, fading to the ocean gradient's own darkest measured tone (Index 1.4). Same technique the pre-tiling single-image approach used for this same problem, adapted to source real edge data from tiles instead of one whole image. |
| 1.7 | Smoothed remaining blocky/banded look in the ocean gradient. Three separate causes: (1) `noWrap` alone (1.5) still let Leaflet request out-of-range tile coordinates, which Vite's dev server answered with its own `index.html` (200 OK) instead of a real 404, rendering as a visible placeholder box - fixed with `bounds={NIGHT_MAP_BOUNDS}` on both `TileLayer`s in `dashboard.tsx`. (2) Independently lossy-compressed adjacent tiles let shared edge pixels drift apart by a few values, invisible on detailed imagery but a visible seam on this mostly-flat gradient once stretched to cover a large screen area at low zoom - fixed by saving tiles and edge-fades lossless (`generate-night-map-tiles.py`, `generate-night-map-edge-fades.py`). (3) 8-bit rounding of a very slowly-changing, very dark gradient produced flat quantization bands with a visible step at each level - fixed with ordered (Bayer) dithering in both generators, plus replacing the edge-fade's hard darkest-of-window (min) smoothing with an added averaging pass so plateau edges ramp instead of stepping, and generating that asset well below its display size so the browser upscales (blends dithering in) rather than downscales it (which cancels ordered dithering back out). |
| 1.8 | Operational Canvas Layer Registry - `CANVAS_LAYERS` in `dashboard.tsx`: the six-layer render stack (Base Map, Operational Layers, Operational Footprint, Country Intelligence, Breathing Markers, Debug Layer Numbers) expressed as an ordered data array, a single source of truth for stacking order instead of only existing implicitly in JSX/CSS z-index. |
| 1.9 | Operational Canvas Calibration Tool - dev-only overlay (`SHOW_CANVAS_CALIBRATION`) that reads live x/y coordinates under the mouse in the same 0-1000 hit-zone space, for aligning country geometry to the approved base map PNG. Measurement aid only, no country/focus logic. |
| 1.10 | Fixed the Calibration Tool (1.9) reporting the Operational Canvas's true centre (y=500) as y=250, regardless of viewport shape - it predated the Operational Focus Block's current 1000x1000 square + "xMidYMid slice" model entirely and divided the vertical axis by a fixed 500. Confirmed directly against a country independently verified pixel-exact centre-screen (New Zealand) being reported as off-centre using this tool, and against the user's own report ("my centre is grid X 500, Y 250"). Both `handleCalibrationMove` (mouse -> canvas) and the crosshair's own screen position (canvas -> mouse, its exact inverse) now share one `getVisibleCanvasRange` helper reproducing the real "cover" scaling every other Operational Canvas overlay already uses. Verified directly: the exact pixel centre of a 1600x900 viewport now reports "Canvas X: 500, Canvas Y: 500". |
| 2.1 | Country Boundary Debug Mode - `SHOW_COUNTRY_BOUNDARIES` in `dashboard.tsx`: draws every `COUNTRY_REGISTRY` boundary at once (thin gold outline, transparent fill) to visually verify alignment against the approved base map. Registry itself always stays loaded for selection/masking regardless of this flag. |
| 2.2 | Country Boundary QA Mode - `SHOW_COUNTRY_QA`: a reviewer-facing tool (distinct from 2.1's all-at-once view) that steps through `COUNTRY_REGISTRY` one country at a time so a single coastline can be inspected without every other outline cluttering the view. Never edits registry data - a wrong-looking country is recorded as a separate review item. |
| 2.2B | Country Registry reprojection - `country-registry.ts` / `generate-country-registry.py`: registry geometry pre-projected into the full square 1000x1000 source-image space (matching `world-map-v17.png`'s 2048x2048 square 1:1), no crop assumption baked in. |
| 2.2C | Operational Geometry Alignment Engine - `OPERATIONAL_GEOMETRY_VIEWBOX` / `OPERATIONAL_GEOMETRY_FIT` (`viewBox="0 0 1000 1000"`, `xMidYMid slice`) shared by every SVG overlay that renders `COUNTRY_REGISTRY` geometry, so invisible hit-zone/clip geometry stays locked to the approved base map's own `object-fit: cover` at any viewport size with no JS resize listener or measured offsets. |
| 3.0 | Operational Country Selection Engine - `country-selection-engine.ts`: the single, permanent entry point for country selection (mouse click today; search/deep-links/AI actions/Camera Engine later all select through this module rather than holding their own state). Stores state and notifies subscribers only - never touches the DOM. Replaced the Index 1.6 hand-typed test zones with real hit zones over `COUNTRY_REGISTRY` geometry for all 235 countries. |
| 3.0A | Selection Debug Badge - `SHOW_SELECTION_DEBUG`: dev-only fixed badge (bottom-right) showing the Active Country on selection, to make the click -> selection update visible without opening devtools. |
| 3.1 / 3.2A | Flat gold-fill focus proof - an early, now-superseded Country Focus rendering: the selected country shown as a flat gold SVG fill/outline rather than the real map image. Superseded by 3.3; no further detail survives beyond this composite reference in the 3.3 code comment. |
| 3.2 | Operational Country Focus Registry - `country-focus-registry.ts`: extends the Country Registry with the data every country transition reads (Focus Point, Camera Target, Default Focus Scale) per country. Data-only module - renders nothing, animates nothing, moves no camera. |
| 3.3 | Operational Country Focus Engine - the country "lifts" from the world map and comes forward rather than zooming like a conventional map product: the selected country is rendered as the real approved map image (`world-map-v17.png`), clipped to its own geometry as a mask only (no gold fill/polygon/coloured overlay/border/glow), sharing the exact viewBox/fit (2.2C) as every other geometry overlay so the cutout aligns pixel-for-pixel before it ever moves. Introduced the staggered entrance sequence (separation fade-in, background dim/blur, scale+move to Camera Target, drop-shadow) and the direction-aware CSS transition pattern (staggered entrance vs. uniform ease-out return) reused by every focus-related style since. |
| 3.4 | Return-to-world-map animation - clearing a selection no longer unmounts the focus layer immediately: it plays a uniform ease-out return transition and only clears the rendered country once that transition has had time to finish, so the cutout/dim/blur have something to animate back to instead of vanishing instantly. Click-outside detection tests against the exact clipped shape, not the raw hit-zone. |
| 3.4A | Two-phase mount timing fix - replaced a nested double-`requestAnimationFrame` mount sequence with a plain macrotask (`setTimeout`). Reproduced a bug where rAF callbacks fired up to ~1s late when a country was selected immediately after clearing a previous one, leaving the new selection stuck invisible at opacity 0. Also guards the return-timeout against a new selection landing in the same tick a stale timer was already due to fire. |
| 3.4C | Clip-path degenerate-ring cleanup - `buildFocusClipPath`: high-vertex-count countries' raw registry geometry (Russia, USA - both tens of thousands of coastline points) include zero-area "ring" artifacts left by the generation pipeline; a single one was enough to corrupt the whole clip mask (reported as USA rendering as a large map-image fragment instead of its own shape). Drops only genuinely zero-area rings, keeps every real ring including the smallest territory, and lightly simplifies (Ramer-Douglas-Peucker) redundant near-duplicate coastline points. Render-time cleanup only, memoised per selection, never touches stored registry data. |
| 3.4I | Per-ring secondary-territory visibility heuristic - an intermediate scaling model that tried to keep a multi-ring country's significant secondary territory (e.g. overseas islands) visible alongside its main landmass via per-ring heuristics layered on the 3.3 model. Superseded by 3.8's Operational Focus Block, which replaced every such heuristic with one fixed-rectangle rule. |
| 3.8 | Operational Focus Block rebuild - replaced every prior per-country/per-ring scaling heuristic (3.3's 45%-of-canvas target, 3.4I's secondary-territory rules) with one fixed rectangle in the shared 1000x1000 Operational Geometry space: every focused country's complete geometry (every ring, every territory, nothing dropped) is scaled uniformly and translated to fit entirely inside it. Re-centred the block on (500,500) - the space's own true visible-at-any-viewport centre - after confirming tall countries (Canada, Russia, South Africa, Australia, USA mainland) rendered cropped at the top under the original off-centre block. Fixed the antimeridian wraparound corrupting scale/focus math for Alaska (falls back to largest-ring-only when the combined bounding box implausibly exceeds 500 units wide) without breaking Hawaii's genuinely tight island cluster (which needs the real combined bounding box, not the largest-ring fallback). Follow-up: split USA mainland, Alaska, and Hawaii into three fully independent, separately-clickable selectable regions instead of a single multi-piece "dominant + insets" layout, and centred Hawaii on its full island chain's true centroid rather than just the Big Island. |
| 3.8A | Country Focus Cutout Kill Switch - `SHOW_COUNTRY_FOCUS_CUTOUT`: flipped off after the pre-rebuild cutout renderer produced disconnected ghost fragments and stray rectangular map-image pieces for multipart countries (USA, Canada, New Zealand, Indonesia). Re-enabled once 3.8's rebuild replaced the underlying data. Kept as a flag (not removed) as a fast, render-level way to fully disable the cutout and its dim/blur background again without touching the registry or Selection Engine. |
| 3.9 | Country Focus entrance animation speedup, 650ms -> 280ms - cut the full entrance sequence (separation fade-in, background dim/blur, scale+move to Camera Target, drop-shadow) after direct feedback that the original timing felt like a delay. |
| 3.10 | Country Focus entrance animation speedup, 280ms -> 140ms - cut further after follow-up feedback that a delay was still perceptible; confirmed via live CSS inspection (not just source-code assumption) that this was genuinely animation duration and not a computational bottleneck - the clip/DOM update itself lands well under 150ms even for the most complex countries. |
| 3.11 | Fixed Country Focus hang for high-zoom small countries - the drop-shadow filter lived on the same element as the country's `transform: scale(N)`, forcing the browser to rasterize the blur at up to N times the resolution to stay crisp at full zoom. This made Dominican Republic (clamped to the `FOCUS_SCALE_MAX = 30` ceiling, same as the earlier Hawaii fix) hang rather than complete its entrance animation; Cuba (scale ~18.46, just under the clamp) was slow for the same reason without fully hanging. Moved the drop-shadow to the unscaled `<svg>` wrapper instead, decoupling the blur's cost from a country's zoom factor. Verified via Playwright/Xvfb across all 237 selectable regions (every country plus the independent Mainland USA/Alaska/Hawaii picks) - all complete in 183-314ms with zero hangs and zero console errors. |
| 3.12 | Removed `FOCUS_SCALE_MAX` entirely - every country now scales to its own true, independent fit-to-block ratio, however small its real footprint is, per direct product direction ("each country zoom scale must be independent" / "make smaller countries bigger"). The 30x ceiling was only ever protecting against 3.11's drop-shadow bug, not scale itself - reproduced and confirmed directly afterward: scale settles in single-digit milliseconds at every value tested up to 3000x with 3.11's fix in place. Surfaced a real, separate tradeoff: countries needing several hundred to a few thousand times zoom (Nauru, Monaco, Aruba) are viewing far less than a single source-image pixel, rendering as a flat, textureless block. Switched on the Map Aesthetics Engine's rim-light border (`MAP_FOCUS_BORDER_VISIBLE`, dormant since 4.1) to keep that reading as a deliberate, premium presentation of a very small place instead of a blank rectangle - which in turn surfaced a real, pre-existing CSS bug (see 4.2's own note) that had been silently breaking the border's colour. One genuine edge case kept a small fallback ceiling: a handful of countries reduce to a truly zero-area bounding box in the registry (currently just Vatican City), where there is no real extent to scale against at all. |
| 3.13 | Generalised the USA-only mainland/Alaska/Hawaii split mechanism (3.8) into a reusable one (`SplitRegionRule` + `COUNTRY_SPLITS` in `country-focus-registry.ts`) and used it to split Spain's Canary Islands (IC/ICA, synthetic - no ISO 3166-1 code of its own) and France's five overseas departments: French Guiana (GF/GUF, real code), Martinique (MQ/MTQ) and Guadeloupe (GP/GLP) - the one split in this file inferred from ring area/shape rather than a clean positional gap, matching real land-area ratios (~1.49 measured vs ~1.44 real) - and Mayotte (YT/MYT) and Réunion (RE/REU), confirmed the same way (measured area ratio ~6.8x vs Réunion's real land area being ~6.7x Mayotte's). A first pass at French Guiana used a single `y > 470` cutoff, which also swept in the far more distant Réunion/Mayotte cluster into one broken region spanning two oceans - caught directly (its clip path started with Réunion's coordinates) and fixed with an added `x < 400` bound. |
| 3.14 | Portrait-shaped countries (Fiji, New Zealand, Chile - all noticeably taller than wide) rendered as thin vertical slivers under the standard uniform fit-to-block rule, since the block itself is landscape-shaped (550x300) and a portrait country's own greater height binds the scale long before its width uses any meaningful fraction of the block's width budget - measured directly: New Zealand's true fit-to-block scale filled only ~23% of the block's width. Per explicit direction ("make an exception for these countries to render bigger"), `scaleToFit` now gives any country whose bounding box is taller than it is wide a taller effective height target (`PORTRAIT_HEIGHT_BUDGET = 400`) to scale against, while its width stays capped at the block's own 550-unit width exactly as before. 400 is deliberately conservative, not the ~562.5 units visible at 16:9 - a country scaled against it stays safely inside the Operational Canvas's always-visible region up to a 21:9 ultrawide display (safe ceiling ~429), verified directly at both 1600x900 and 2560x1080. |
| 3.15 | Chile reported rendering visibly off-centre - its combined registry bounding box (including a distant Pacific ring, almost certainly Easter Island, ~5.3x the mainland ring's own bbox area) pulled the focus point west of the actual visible mainland and shrank the scale to accommodate the empty ocean between them. `buildGenericFocusDefinition` (every ordinary, non-split country) had no correction for this - only the split-region path (3.8's Alaska antimeridian fix) had a narrow, one-off width-threshold check. Generalised that into `computeScaleReferenceBox`, shared by both: starting from a country's largest ring, greedily absorb every other ring (largest-first) into a running "core" box as long as doing so doesn't multiply the box's area by more than 4x - a nearby island barely grows the box and is absorbed; a genuinely distant, disconnected outlier would blow the box up far past what its own real close territory needs, and is excluded from the focus/scale reference only (still renders in the country's own `svgPath`). Verified directly: Chile's focus point moved to match the mainland ring's own centre almost exactly; Portugal (Azores/Madeira) benefited the same way; Alaska/Hawaii/mainland USA (the cases 3.8's narrower check already handled) are unaffected. |
| 3.16 | Swept the full registry for the same distant-outlier pattern that broke Chile (3.15), then checked each candidate against real geography before acting - most of the ~30 raw candidates (Kiribati, Micronesia, Marshall Islands, French Polynesia, Solomon Islands, Bahamas, etc.) turned out to be self-contained, naturally scattered island nations rather than a different country's exclave, so there is no distinct real place to name a split after; left as single countries. Seven were the genuine pattern, each verified individually the way 3.13's French/Spanish splits were: Russia -> Kaliningrad (KA/KGD, synthetic - the exclave separated by Lithuania/Poland/Belarus), Norway -> Svalbard (SJ/SJM, real code - ~55 units north of mainland's own northernmost point with no gap in between), Australia -> Tasmania (TS/TAS, synthetic), Ecuador -> Galápagos Islands (GX/GAL, synthetic), Portugal -> Madeira (MI/MAD) and Azores (AC/AZO), distinguished from each other and from mainland by latitude/longitude, and Chile -> Easter Island (EI/EAS) - the same distant ring that caused 3.15's original off-centre report. Same `SplitRegionRule`/`COUNTRY_SPLITS` mechanism as 3.13, no new machinery. |
| 4.1 | Map & Country Focus Aesthetics Engine scaffold (`map-aesthetics.ts`) - centralises the Operational Canvas's own visual identity (ocean background, grid/boundary treatment, accent border colour) in one place instead of scattered inline values across `dashboard.tsx`/`index.css`. Scope is the map and Country Focus cutout only, per direct product direction - the rest of the app's UI (sign-in, brief, QA chrome) gets its own aesthetics pass later. Landed as infrastructure only: every value reproduced VenueGuard's existing look exactly (same grid visibility, same accent colour, same flat ocean background, no border on the focus cutout) - nothing rendered differently yet, so the actual aesthetic pass could be applied afterward one deliberate step at a time. |
| 4.2 | Removed the Country Boundary QA Mode's (2.2) default single-country highlight (`qaShowOutline`, defaulted to on) - since it starts on the registry's first country, this showed as a stray gold border around Afghanistan on the world map and behind whatever country was actually clicked into, reported directly ("Afghanistan has a border colour which is present in the world map"). Defaults to off now; the checkbox still lets a reviewer turn it back on. Turning the Engine's own rim-light border on later (3.12) surfaced a real, pre-existing CSS bug this same styling pattern had: the accent colour custom property is stored comma-separated (`"255, 196, 87"`), but both this mode's stroke rule and the boundary debug grid's (2.1) used the modern `rgb(var(...) / alpha)` slash syntax, invalid CSS when mixed with a comma-separated value - it silently computed to `stroke: none`, quietly breaking the grid's colour unnoticed until the border made it obvious. Fixed by switching both rules to `rgba(var(...), alpha)`, the legacy syntax that actually composes with a comma-separated custom property. |
| 4.3 | Country Focus cutout edges only read as crisp for large countries (Russia, Australia) that barely zoom to fill the Operational Focus Block - 3.4C's clip-path simplification tolerance was tuned in world-space units, invisible at their ~1x zoom but magnified right along with everything else for small countries needing much more zoom (Cuba ~18x, Dominican Republic ~30x), turning imperceptible simplification corners into visibly chunky, low-poly coastline facets. First pass divided the tolerance by the country's own focus scale, keeping the on-screen error roughly constant instead of constant in world-space (Cuba's clip path improved from keeping 56 of 106 raw points, 53%, to 87, 82%). |
| 4.4 | 4.3's fix still thinned every country's coastline somewhat. Measured the real registry data directly instead of continuing to guess at a formula: outside of Russia's mainland ring (~4,894 points), Canada's (~3,317), and USA's two largest (~1,988/~1,618), no country's largest ring exceeds ~363 points (Indonesia) - nowhere near a performance concern regardless of zoom. Added `MAP_BORDER_FULL_DETAIL_MAX_POINTS` (600) to the Engine: any ring at or under that count now renders at full, unsimplified fidelity - Cuba's clip path keeps all 106 of its points. Only rings above the threshold (the 3 giants) still fall back to 4.3's scale-aware thinning, and those all render at close to 1x zoom anyway, so nothing changes for them. Verified across all 237 selectable regions: still 0 hangs, 0 renders over 800ms, 0 console errors, including Russia (351ms), Canada (273ms), USA (301ms), and Alaska (227ms) specifically. |
| 4.5 | Map Aesthetics Engine's (4.1) first applied change: Country Boundary Debug Mode's all-countries grid (`MAP_GRID_VISIBLE`, `map-aesthetics.ts`) switched off by default - the first Layer 4 aesthetic pass value actually changed from its original default, since Index 2.1's grid was a verification aid only and was never meant to ship visible. |
| 5.1 | Major cities layer - `CITY_REGISTRY` (`city-registry.ts`, generated by `generate-city-registry.py` from Natural Earth's `ne_10m_populated_places_simple.geojson`) renders each selected country's capital and top-population cities (name + small dot) inside the Country Focus cutout, sharing the exact Web Mercator projection formula and constant-screen-size inverse-scale transform already established for country geometry. Deliberately quiet (no glow, no animation, no click behaviour) - kept visually subordinate to a later, operationally-meaningful presence/office layer discussed but not yet built. Data-level dedupe (`dedupe_by_name`) added after Niger's Niamey appeared twice in the source dataset (the real capital plus a lower-population duplicate ~14 units away), preferring the capital-flagged or higher-population record. |
| 5.2 | Prevented major-city labels (5.1) from overlapping each other, reported directly - `selectNonOverlappingCities` in `dashboard.tsx`. Since labels render at a constant screen size (inverse-scale) while city positions still scale with the country's own zoom, two geographically close cities can land close enough on screen to collide, worst at low zoom for large countries with clustered cities. Greedy placement in priority order (capital first, then population descending), rejecting any city within 46px (screen space, reusing the Calibration Tool's own px-per-unit convention) of an already-placed one; rejected cities drop entirely, dot and label both. Verified via Playwright against the closest real clusters in the generated data (Egypt's Cairo/Giza, South Africa's Johannesburg/Benoni/Pretoria) plus a full 251-region zero-error sweep. |
| 6.1 | Slide-out panel system, Layer 6 - `PROJECT_CONTEXT.md`'s locked "All additional information is presented through slide-out panels" principle, first applied: the Operational Brief panel (`.brief-panel`, `dashboard.tsx`/`index.css`) recalls the same Operational Brief content shown at the mandatory pre-entry gate (`step === "brief"`), on demand, inside the Operations Centre. A Product Constitution conflict was caught and stopped before implementation - "Brief as a slide-out panel instead of the mandatory gate" would have broken the locked Product Promise ("Every operator begins with an Operational Brief before entering the Operations Centre") - resolved as "Keep both": the gate stays exactly as-is, the panel is additive only. Both now read from one shared `OPERATIONAL_BRIEF` constant so they can't drift out of sync. Non-modal by design (no dimming overlay) so the map stays the primary focus while open; slides from the left edge, trigger docks top-left. |
| 6.2 | Alerts panel (`.alerts-panel`) - individual timestamped events (critical/warning/info severity), distinct from the Brief's (6.1) standing daily summary. Mirrors the Brief panel's non-modal treatment but slides from the right edge, so the two can be open together without competing for the same screen space. |
| 6.3 | Country Intelligence panel, first pass (`.country-panel`) - tied to Active Country selection rather than a manual trigger, mirroring the Country Focus visuals' own entered/exited timing (`focusEntered`) so it appears and dismisses in lockstep with the selected country. Shows real data where available (capital and city count, from the City Registry, 5.1) and mock risk-level/advisory data otherwise, deterministic per ISO3 rather than fabricated per visit. Initial layout: a card centred at the bottom of the canvas. |
| 6.4 | Moved the Country Intelligence panel (6.3) from bottom-centre to the right edge, matching the Alerts panel's (6.2) treatment - the bottom-centre card was covering large parts of smaller selected countries, reported directly. Shifts left by one panel-width when the Alerts panel is also open, so the two sit side by side instead of overlapping. |
| 6.5 | Hid the Active Country debug badge (`SHOW_SELECTION_DEBUG`, Index 3.0A) by default - the same dev-only click-event proof already hidden behind the Calibration Tool (1.9) and Country Boundary QA Mode (2.2), now off by default too since the Country Intelligence panel (6.3) already surfaces this information in the product itself. |
| 6.6 | Operational Layers panel (`.layers-panel`) - a legend/control panel for the canvas itself, listing Base Map (fixed/always-on), Major Cities (5.1), Country Focus Fill, Country Focus Border, and the Country Boundary Grid (4.5), each with a live toggle switch. Converted four module-level display flags to component state seeded from their existing defaults (`map-aesthetics.ts` / this file's own flags), so a toggle only changes the current session - it never writes back to those defaults, which still govern what a fresh session looks like. Docks left alongside the Brief panel (6.1), shifting right when Brief is also open; trigger stacks below Brief's in a shared `.canvas-trigger-stack`. |
| 6.7 | Persistent top banner (`.top-banner`) above the Operations Centre - VenueGuard branding and "Operations Centre" context on the left, operator identity and Sign Out on the right. Takes up permanent screen space (the canvas sits below it in a flex column) rather than floating over the map like the panel triggers, per direct product direction. Required changing `.operational-canvas`'s height from a hardcoded `100vh` to `100%` of its flex parent, so it fills exactly the space below the banner instead of overflowing past it. |
| 6.8 | Fixed the Operational Brief panel (6.1) failing to close whenever the Layers panel (6.6) was closed, reported directly. The Layers panel's `-shifted` position class was keyed only on the Brief panel being open, not on the Layers panel's own open state. A closed panel's off-screen `translateX` is computed relative to its own positioning origin; shifting that origin while still closed moved its "hidden" position back on screen - invisible, but still `pointer-events: auto` and later in the DOM, so it silently intercepted every click meant for the Brief panel underneath it, including its own close button. Fixed by only applying a panel's `-shifted` class when the panel itself is open; the identical latent bug in the Country Intelligence panel's (6.4) Alerts-triggered shift was fixed the same way. |
